--[[
	Nova Hub Key System
	Credits: SGA x AHMAD
]]

repeat task.wait() until game:IsLoaded()

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local LocalPlayer = Players.LocalPlayer

local CORRECT_KEY = "NovaHub"
local GET_KEY_LINK = "https://discord.gg/3kS2fc5mUX"
local MAX_ATTEMPTS = 5
local LOCKOUT_SECONDS = 45

getgenv().KEY_PASSED = false
getgenv().NovaHub_KeyAttempts = getgenv().NovaHub_KeyAttempts or 0
getgenv().NovaHub_KeyLockUntil = getgenv().NovaHub_KeyLockUntil or 0

-- Theme colors
local THEMES = {
	Crimson = {
		bg = Color3.fromRGB(18, 8, 10),
		panel = Color3.fromRGB(32, 12, 16),
		accent = Color3.fromRGB(220, 20, 60),
		accent2 = Color3.fromRGB(139, 0, 30),
		text = Color3.fromRGB(255, 235, 238),
		muted = Color3.fromRGB(180, 140, 150),
		input = Color3.fromRGB(40, 18, 22),
		btn = Color3.fromRGB(55, 18, 28),
		rainbow = false,
	},
	Blue = {
		bg = Color3.fromRGB(8, 12, 22),
		panel = Color3.fromRGB(14, 22, 40),
		accent = Color3.fromRGB(30, 144, 255),
		accent2 = Color3.fromRGB(0, 80, 180),
		text = Color3.fromRGB(230, 240, 255),
		muted = Color3.fromRGB(140, 160, 200),
		input = Color3.fromRGB(18, 28, 48),
		btn = Color3.fromRGB(20, 40, 70),
		rainbow = false,
	},
	Rainbow = {
		bg = Color3.fromRGB(12, 12, 14),
		panel = Color3.fromRGB(22, 22, 26),
		accent = Color3.fromRGB(255, 80, 120),
		accent2 = Color3.fromRGB(80, 120, 255),
		text = Color3.fromRGB(255, 255, 255),
		muted = Color3.fromRGB(170, 170, 180),
		input = Color3.fromRGB(30, 30, 36),
		btn = Color3.fromRGB(40, 40, 48),
		rainbow = true,
	},
	Red = {
		bg = Color3.fromRGB(16, 6, 6),
		panel = Color3.fromRGB(28, 10, 10),
		accent = Color3.fromRGB(255, 40, 40),
		accent2 = Color3.fromRGB(160, 0, 0),
		text = Color3.fromRGB(255, 240, 240),
		muted = Color3.fromRGB(200, 150, 150),
		input = Color3.fromRGB(36, 14, 14),
		btn = Color3.fromRGB(50, 16, 16),
		rainbow = false,
	},
	Purple = {
		bg = Color3.fromRGB(14, 8, 22),
		panel = Color3.fromRGB(26, 14, 42),
		accent = Color3.fromRGB(170, 80, 255),
		accent2 = Color3.fromRGB(100, 30, 180),
		text = Color3.fromRGB(245, 235, 255),
		muted = Color3.fromRGB(170, 150, 200),
		input = Color3.fromRGB(32, 18, 50),
		btn = Color3.fromRGB(45, 24, 70),
		rainbow = false,
	},
}

local currentThemeName = "Crimson"
local theme = THEMES[currentThemeName]

local function parentGui()
	local ok, hui = pcall(function() return gethui and gethui() end)
	if ok and hui then return hui end
	return CoreGui
end

-- destroy old
pcall(function()
	local p = parentGui()
	local old = p:FindFirstChild("NovaHubKeySystem")
	if old then old:Destroy() end
end)

local Gui = Instance.new("ScreenGui")
Gui.Name = "NovaHubKeySystem"
Gui.ResetOnSpawn = false
Gui.IgnoreGuiInset = true
Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
Gui.DisplayOrder = 9999
pcall(function() Gui.Parent = parentGui() end)
if not Gui.Parent then
	Gui.Parent = LocalPlayer:WaitForChild("PlayerGui")
end

-- Dim backdrop
local Dim = Instance.new("Frame")
Dim.Name = "Dim"
Dim.Size = UDim2.fromScale(1, 1)
Dim.BackgroundColor3 = Color3.new(0, 0, 0)
Dim.BackgroundTransparency = 0.45
Dim.BorderSizePixel = 0
Dim.Parent = Gui

local Main = Instance.new("Frame")
Main.Name = "Main"
Main.Size = UDim2.fromOffset(420, 340)
Main.Position = UDim2.new(0.5, -210, 0.5, -170)
Main.BackgroundColor3 = theme.bg
Main.BorderSizePixel = 0
Main.Active = true
Main.Parent = Gui

local MainCorner = Instance.new("UICorner", Main)
MainCorner.CornerRadius = UDim.new(0, 16)

local Glow = Instance.new("UIStroke", Main)
Glow.Thickness = 2.2
Glow.Color = theme.accent
Glow.Transparency = 0.15

local TopBar = Instance.new("Frame", Main)
TopBar.Size = UDim2.new(1, 0, 0, 52)
TopBar.BackgroundColor3 = theme.panel
TopBar.BorderSizePixel = 0
Instance.new("UICorner", TopBar).CornerRadius = UDim.new(0, 16)

local TopFix = Instance.new("Frame", TopBar)
TopFix.Size = UDim2.new(1, 0, 0, 16)
TopFix.Position = UDim2.new(0, 0, 1, -16)
TopFix.BackgroundColor3 = theme.panel
TopFix.BorderSizePixel = 0

local Title = Instance.new("TextLabel", TopBar)
Title.Size = UDim2.new(1, -20, 1, 0)
Title.Position = UDim2.fromOffset(16, 0)
Title.BackgroundTransparency = 1
Title.Text = "Nova Hub Key System"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 20
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.TextColor3 = theme.text

local Sub = Instance.new("TextLabel", Main)
Sub.Size = UDim2.new(1, -32, 0, 22)
Sub.Position = UDim2.fromOffset(16, 58)
Sub.BackgroundTransparency = 1
Sub.Text = "Enter key to unlock Muscle Legends script"
Sub.Font = Enum.Font.Gotham
Sub.TextSize = 13
Sub.TextXAlignment = Enum.TextXAlignment.Left
Sub.TextColor3 = theme.muted

-- Color selector
local ColorLabel = Instance.new("TextLabel", Main)
ColorLabel.Size = UDim2.new(1, -32, 0, 18)
ColorLabel.Position = UDim2.fromOffset(16, 88)
ColorLabel.BackgroundTransparency = 1
ColorLabel.Text = "THEME COLOR"
ColorLabel.Font = Enum.Font.GothamBold
ColorLabel.TextSize = 11
ColorLabel.TextXAlignment = Enum.TextXAlignment.Left
ColorLabel.TextColor3 = theme.muted

local ColorRow = Instance.new("Frame", Main)
ColorRow.Size = UDim2.new(1, -32, 0, 34)
ColorRow.Position = UDim2.fromOffset(16, 110)
ColorRow.BackgroundTransparency = 1

local colorOrder = {"Crimson", "Blue", "Rainbow", "Red", "Purple"}
local colorDots = {}

local function applyTheme(name)
	currentThemeName = name
	theme = THEMES[name]
	Main.BackgroundColor3 = theme.bg
	TopBar.BackgroundColor3 = theme.panel
	TopFix.BackgroundColor3 = theme.panel
	Title.TextColor3 = theme.text
	Sub.TextColor3 = theme.muted
	ColorLabel.TextColor3 = theme.muted
	Glow.Color = theme.accent
	for n, dot in pairs(colorDots) do
		dot.UIStroke.Thickness = (n == name) and 2.5 or 1
		dot.UIStroke.Color = (n == name) and Color3.new(1, 1, 1) or Color3.fromRGB(80, 80, 80)
	end
end

for i, name in ipairs(colorOrder) do
	local t = THEMES[name]
	local btn = Instance.new("TextButton", ColorRow)
	btn.Size = UDim2.fromOffset(34, 34)
	btn.Position = UDim2.fromOffset((i - 1) * 42, 0)
	btn.BackgroundColor3 = t.accent
	btn.Text = ""
	btn.AutoButtonColor = false
	Instance.new("UICorner", btn).CornerRadius = UDim.new(1, 0)
	local st = Instance.new("UIStroke", btn)
	st.Thickness = 1
	st.Color = Color3.fromRGB(80, 80, 80)
	btn.UIStroke = st
	colorDots[name] = btn
	btn.MouseButton1Click:Connect(function()
		applyTheme(name)
	end)
end
applyTheme("Crimson")

-- Key input
local Box = Instance.new("TextBox", Main)
Box.Size = UDim2.new(1, -32, 0, 44)
Box.Position = UDim2.fromOffset(16, 160)
Box.PlaceholderText = "ENTER KEY"
Box.Text = ""
Box.Font = Enum.Font.Gotham
Box.TextSize = 15
Box.BackgroundColor3 = theme.input
Box.TextColor3 = theme.text
Box.PlaceholderColor3 = theme.muted
Box.ClearTextOnFocus = false
Box.TextXAlignment = Enum.TextXAlignment.Center
Instance.new("UICorner", Box).CornerRadius = UDim.new(0, 10)
local BoxStroke = Instance.new("UIStroke", Box)
BoxStroke.Thickness = 1.2
BoxStroke.Color = theme.accent2
BoxStroke.Transparency = 0.4

local Status = Instance.new("TextLabel", Main)
Status.Size = UDim2.new(1, -32, 0, 20)
Status.Position = UDim2.fromOffset(16, 210)
Status.BackgroundTransparency = 1
Status.Text = ""
Status.Font = Enum.Font.Gotham
Status.TextSize = 12
Status.TextColor3 = theme.accent

local Verify = Instance.new("TextButton", Main)
Verify.Size = UDim2.new(0.46, -8, 0, 42)
Verify.Position = UDim2.new(0, 16, 0, 240)
Verify.Text = "VERIFY"
Verify.Font = Enum.Font.GothamBold
Verify.TextSize = 14
Verify.TextColor3 = theme.text
Verify.BackgroundColor3 = theme.btn
Verify.AutoButtonColor = false
Instance.new("UICorner", Verify).CornerRadius = UDim.new(0, 10)
local VStroke = Instance.new("UIStroke", Verify)
VStroke.Thickness = 1.2
VStroke.Color = theme.accent

local GetKey = Instance.new("TextButton", Main)
GetKey.Size = UDim2.new(0.46, -8, 0, 42)
GetKey.Position = UDim2.new(0.54, 0, 0, 240)
GetKey.Text = "GET KEY"
GetKey.Font = Enum.Font.GothamBold
GetKey.TextSize = 14
GetKey.TextColor3 = theme.text
GetKey.BackgroundColor3 = theme.btn
GetKey.AutoButtonColor = false
Instance.new("UICorner", GetKey).CornerRadius = UDim.new(0, 10)
local GStroke = Instance.new("UIStroke", GetKey)
GStroke.Thickness = 1.2
GStroke.Color = theme.accent2

local Footer = Instance.new("TextLabel", Main)
Footer.Size = UDim2.new(1, -20, 0, 18)
Footer.Position = UDim2.new(0, 10, 1, -28)
Footer.BackgroundTransparency = 1
Footer.Text = "Credits: SGA x AHMAD  ·  attempts are limited"
Footer.Font = Enum.Font.Gotham
Footer.TextSize = 11
Footer.TextColor3 = theme.muted

-- Drag
do
	local dragging, start, startPos
	TopBar.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			start = input.Position
			startPos = Main.Position
		end
	end)
	TopBar.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = false
		end
	end)
	UserInputService.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			local d = input.Position - start
			Main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + d.X, startPos.Y.Scale, startPos.Y.Offset + d.Y)
		end
	end)
end

-- Rainbow / accent pulse
local hue = 0
RunService.RenderStepped:Connect(function(dt)
	if not Gui.Parent then return end
	if theme.rainbow then
		hue = (hue + dt * 0.25) % 1
		local c = Color3.fromHSV(hue, 0.9, 1)
		Glow.Color = c
		VStroke.Color = c
		BoxStroke.Color = Color3.fromHSV((hue + 0.3) % 1, 0.8, 1)
		if colorDots.Rainbow then
			colorDots.Rainbow.BackgroundColor3 = c
		end
	else
		-- soft pulse
		local a = 0.12 + 0.08 * math.sin(os.clock() * 2.2)
		Glow.Transparency = a
	end
	-- live theme paint for dynamic widgets
	Box.BackgroundColor3 = theme.input
	Box.TextColor3 = theme.text
	Verify.BackgroundColor3 = theme.btn
	GetKey.BackgroundColor3 = theme.btn
	Verify.TextColor3 = theme.text
	GetKey.TextColor3 = theme.text
	Footer.TextColor3 = theme.muted
	Status.TextColor3 = theme.accent
	VStroke.Color = theme.rainbow and VStroke.Color or theme.accent
	GStroke.Color = theme.accent2
	BoxStroke.Color = theme.rainbow and BoxStroke.Color or theme.accent2
end)

local function Shake()
	local base = Main.Position
	for i = 1, 10 do
		Main.Position = base + UDim2.fromOffset((i % 2 == 0) and -7 or 7, 0)
		task.wait(0.018)
	end
	Main.Position = base
end

local function setStatus(msg, bad)
	Status.Text = msg
	Status.TextColor3 = bad and Color3.fromRGB(255, 90, 90) or theme.accent
end

local function isLocked()
	return os.clock() < (getgenv().NovaHub_KeyLockUntil or 0)
end

local function onSuccess()
	getgenv().KEY_PASSED = true
	setStatus("KEY ACCEPTED — loading Nova Hub...", false)
	TweenService:Create(Main, TweenInfo.new(0.45, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
		BackgroundTransparency = 1,
		Size = UDim2.fromOffset(380, 300),
	}):Play()
	TweenService:Create(Dim, TweenInfo.new(0.45), { BackgroundTransparency = 1 }):Play()
	for _, v in ipairs(Main:GetDescendants()) do
		if v:IsA("TextLabel") or v:IsA("TextButton") or v:IsA("TextBox") then
			TweenService:Create(v, TweenInfo.new(0.4), {
				TextTransparency = 1,
				BackgroundTransparency = 1,
			}):Play()
		elseif v:IsA("UIStroke") then
			TweenService:Create(v, TweenInfo.new(0.4), { Transparency = 1 }):Play()
		elseif v:IsA("Frame") then
			TweenService:Create(v, TweenInfo.new(0.4), { BackgroundTransparency = 1 }):Play()
		end
	end
	task.wait(0.55)
	pcall(function() Gui:Destroy() end)
end

Verify.MouseButton1Click:Connect(function()
	if isLocked() then
		local left = math.ceil(getgenv().NovaHub_KeyLockUntil - os.clock())
		setStatus("LOCKED — wait " .. left .. "s", true)
		Shake()
		return
	end

	local typed = tostring(Box.Text or ""):gsub("^%s+", ""):gsub("%s+$", "")
	-- tough checks: exact match, no empty, length gate
	if typed == "" then
		setStatus("Enter a key first", true)
		Shake()
		return
	end
	if #typed < 4 then
		setStatus("Key too short", true)
		Shake()
		return
	end
	if typed == CORRECT_KEY then
		onSuccess()
		return
	end

	-- failed
	getgenv().NovaHub_KeyAttempts = (getgenv().NovaHub_KeyAttempts or 0) + 1
	local left = MAX_ATTEMPTS - getgenv().NovaHub_KeyAttempts
	Box.Text = ""
	Box.PlaceholderText = "WRONG KEY"
	if left <= 0 then
		getgenv().NovaHub_KeyLockUntil = os.clock() + LOCKOUT_SECONDS
		getgenv().NovaHub_KeyAttempts = 0
		setStatus("Too many attempts — locked " .. LOCKOUT_SECONDS .. "s", true)
	else
		setStatus("Wrong key — " .. left .. " attempts left", true)
	end
	Shake()
end)

GetKey.MouseButton1Click:Connect(function()
	local ok = false
	for _, fn in ipairs({
		function() setclipboard(GET_KEY_LINK) end,
		function() toclipboard(GET_KEY_LINK) end,
		function() set_clipboard(GET_KEY_LINK) end,
	}) do
		if pcall(fn) then ok = true break end
	end
	setStatus(ok and "Discord invite copied" or GET_KEY_LINK, false)
end)

Box.FocusLost:Connect(function(enter)
	if enter then
		-- trigger verify
		pcall(function() Verify.MouseButton1Click:Fire() end)
		-- fallback direct
		if not getgenv().KEY_PASSED then
			local typed = tostring(Box.Text or ""):gsub("^%s+", ""):gsub("%s+$", "")
			if typed == CORRECT_KEY and not isLocked() then
				onSuccess()
			end
		end
	end
end)

-- hover
for _, btn in ipairs({Verify, GetKey}) do
	btn.MouseEnter:Connect(function()
		TweenService:Create(btn, TweenInfo.new(0.15), {
			BackgroundColor3 = theme.accent2,
		}):Play()
	end)
	btn.MouseLeave:Connect(function()
		TweenService:Create(btn, TweenInfo.new(0.15), {
			BackgroundColor3 = theme.btn,
		}):Play()
	end)
end

-- Intro scale
Main.Size = UDim2.fromOffset(0, 0)
TweenService:Create(Main, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
	Size = UDim2.fromOffset(420, 340),
}):Play()

repeat task.wait(0.05) until getgenv().KEY_PASSED == true

-- ===== KEY PASSED — load Nova Hub Muscle Legends =====

-- Nova Hub | Muscle Legends | Made by SGA x AHMAD

local getgenv = getgenv or function()
    return _G
end

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualUser = game:GetService("VirtualUser")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local RemoteEvents = ReplicatedStorage:WaitForChild("rEvents", 5)
local MuscleEvent = LocalPlayer:FindFirstChild("muscleEvent")
    or ReplicatedStorage:FindFirstChild("muscleEvent")
    or ReplicatedStorage:WaitForChild("muscleEvent", 5)

local PetShopFolder = ReplicatedStorage:FindFirstChild("cPetShopFolder")
local PetShopRemote = ReplicatedStorage:FindFirstChild("cPetShopRemote")

getgenv().autoRep = false
getgenv().autoMachineLift = false
getgenv().autoRebirth = false
getgenv().autoBuyPet = false
getgenv().autoHatch = false
getgenv().autoSell = false
getgenv().autoEvolve = false
getgenv().autoTitan = false
getgenv().autoQuest = false
getgenv().autoSpin = false
getgenv().doubleEquip = false
getgenv().evolveMode = "Both"
getgenv().titanMode = "Both"
getgenv().punchTiny = false
getgenv().punchFrozen = false
getgenv().punchMythical = false
getgenv().punchHell = false
getgenv().punchLegend = false
getgenv().punchMuscleKing = false
getgenv().punchJungle = false
getgenv().punchIndustrial = false
getgenv().autoKillSelected = false
getgenv().autoKillEveryone = false
getgenv().selectedKillPlayer = nil
getgenv().selectedSpectatePlayer = nil
getgenv().selectedWhitelistPlayer = nil
getgenv().whitelistNames = {}

local startTime = os.time()
local uniquePetsEquipped = false
local omegaPetsEquipped = false

local gyms = {
    {"Spawn", CFrame.new(2, 8, 115)},
    {"Secret Spot", CFrame.new(1947, 2, 6191)},
    {"Tiny Island", CFrame.new(-34, 7, 1903)},
    {"Frozen Island", CFrame.new(-2600, 4, -404)},
    {"Mythical Island", CFrame.new(2255, 7, 1071)},
    {"Hell Island", CFrame.new(-6768, 7, -1287)},
    {"Legend Island", CFrame.new(4604, 991, -3887)},
    {"Muscle King Island", CFrame.new(-8646, 17, -5738)},
    {"Jungle Island", CFrame.new(-8659, 6, 2384)},
    {"Industrial Island", CFrame.new(-5437.2, 60.5, 4929.2)},
    {"Brawl Lava", CFrame.new(4471, 119, -8836)},
    {"Brawl Desert", CFrame.new(960, 17, -7398)},
    {"Brawl Regular", CFrame.new(-1849, 20, -6335)}
}

LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

local function createAfkDisplay(character)
    if not character then
        return
    end
    local head = character:WaitForChild("Head", 5)
    if not head or head:FindFirstChild("AFK_Tag") then
        return
    end
    local billboard = Instance.new("BillboardGui")
    billboard.Name = "AFK_Tag"
    billboard.Size = UDim2.new(0, 170, 0, 28)
    billboard.StudsOffset = Vector3.new(0, 3, 0)
    billboard.AlwaysOnTop = true
    billboard.Parent = head
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = "Nova Hub ANTI-AFK [00:00:00]"
    label.TextColor3 = Color3.fromRGB(0, 255, 200)
    label.Font = Enum.Font.GothamBold
    label.TextSize = 12
    label.TextStrokeTransparency = 0.2
    label.Parent = billboard
    local stroke = Instance.new("UIStroke")
    stroke.Thickness = 1.4
    stroke.Color = Color3.fromRGB(0, 0, 0)
    stroke.Parent = label
    task.spawn(function()
        while billboard and billboard.Parent do
            local e = os.time() - startTime
            local h = math.floor(e / 3600)
            local m = math.floor((e % 3600) / 60)
            local s = e % 60
            label.Text = string.format("Nova Hub ANTI-AFK [%02d:%02d:%02d]", h, m, s)
            task.wait(1)
        end
    end)
end

if LocalPlayer.Character then
    createAfkDisplay(LocalPlayer.Character)
end
LocalPlayer.CharacterAdded:Connect(createAfkDisplay)

local function isProteinOrConsumable(name)
    local n = string.lower(tostring(name or ""))
    return string.find(n, "protein")
        or string.find(n, "shake")
        or string.find(n, "potion")
        or string.find(n, "boost")
        or string.find(n, "food")
        or string.find(n, "drink")
end

local function getEquippedTool()
    local char = LocalPlayer.Character
    if not char then
        return
    end
    local tool = char:FindFirstChildOfClass("Tool")
    if tool then
        local n = string.lower(tool.Name)
        if not string.find(n, "punch") and not isProteinOrConsumable(tool.Name) then
            return tool
        end
    end
    local backpack = LocalPlayer:FindFirstChild("Backpack")
    if backpack then
        for _, item in ipairs(backpack:GetChildren()) do
            if item:IsA("Tool") then
                local n = string.lower(item.Name)
                if not string.find(n, "punch") and not isProteinOrConsumable(item.Name) then
                    local hum = char:FindFirstChildOfClass("Humanoid")
                    if hum then
                        hum:EquipTool(item)
                    end
                    return item
                end
            end
        end
    end
    return nil
end

local function unequipProtein()
    pcall(function()
        local char = LocalPlayer.Character
        if not char then
            return
        end
        local tool = char:FindFirstChildOfClass("Tool")
        if tool and isProteinOrConsumable(tool.Name) then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum:UnequipTools()
            end
        end
    end)
end

local function equipPunch()
    local char = LocalPlayer.Character
    local backpack = LocalPlayer:FindFirstChild("Backpack")
    if not char then
        return nil
    end
    local punch = char:FindFirstChild("Punch")
    if punch then
        return punch
    end
    if backpack then
        punch = backpack:FindFirstChild("Punch")
        if punch then
            char.Humanoid:EquipTool(punch)
            return punch
        end
    end
    return nil
end

local function massTogglePets(rarity, state)
    pcall(function()
        local equipRemote = RemoteEvents:FindFirstChild("equipPetEvent")
        local petsFolder = LocalPlayer:FindFirstChild("petsFolder")
        local rarityFolder = petsFolder and petsFolder:FindFirstChild(rarity)
        if equipRemote and rarityFolder then
            for _, pet in ipairs(rarityFolder:GetChildren()) do
                equipRemote:FireServer(state and "equipPet" or "unequipPet", pet)
                task.wait(0.02)
            end
        end
    end)
end

local function massSellPets(rarity)
    pcall(function()
        local sellRemote = RemoteEvents:FindFirstChild("sellPetEvent")
        local petsFolder = LocalPlayer:FindFirstChild("petsFolder")
        local rarityFolder = petsFolder and petsFolder:FindFirstChild(rarity)
        if sellRemote and rarityFolder then
            for _, pet in ipairs(rarityFolder:GetChildren()) do
                sellRemote:FireServer("sellPet", pet)
            end
        end
    end)
end

local function teleportTo(cframe)
    local char = LocalPlayer.Character
    if not char then
        return
    end
    local root = char:FindFirstChild("HumanoidRootPart")
    if root then
        root.CFrame = cframe
    end
end

local function getRock(rockName)
    local folder = workspace:FindFirstChild("machinesFolder")
    if not folder then
        return nil
    end
    local rock = folder:FindFirstChild(rockName)
    if rock then
        return rock
    end
    for _, obj in ipairs(folder:GetChildren()) do
        if string.find(string.lower(obj.Name), string.lower(rockName)) then
            return obj
        end
    end
    return nil
end

local function getRockCFrame(rock)
    if not rock then
        return nil
    end
    if rock:IsA("Model") then
        local primary = rock.PrimaryPart or rock:FindFirstChildWhichIsA("BasePart")
        if primary then
            return primary.CFrame
        end
    elseif rock:IsA("BasePart") then
        return rock.CFrame
    end
    for _, part in ipairs(rock:GetDescendants()) do
        if part:IsA("BasePart") then
            return part.CFrame
        end
    end
    return nil
end

local function punchRockLoop(flagName, rockName)
    task.spawn(function()
        while getgenv()[flagName] do
            pcall(function()
                local rock = getRock(rockName)
                local rockCF = getRockCFrame(rock)
                local char = LocalPlayer.Character
                local root = char and char:FindFirstChild("HumanoidRootPart")
                if rockCF and root then
                    root.CFrame = rockCF * CFrame.new(0, 0, 4)
                end
                local punch = equipPunch()
                if punch then
                    punch:Activate()
                end
                if MuscleEvent then
                    MuscleEvent:FireServer("punch")
                    MuscleEvent:FireServer("rep")
                end
            end)
            task.wait(0.12)
        end
    end)
end

local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "Nova Hub | Muscle Legends",
    Author = "SGA x AHMAD",
    Folder = "NovaHubConfigs",
    Size = UDim2.fromOffset(580, 520),
    Transparent = true,
    Theme = "Dark"
})

local MainTab = Window:Tab({Title = "Main", Icon = "home"})
local PackFarmTab = Window:Tab({Title = "PACK FARM", Icon = "package"})
local PetTab = Window:Tab({Title = "Pets", Icon = "paw-print"})
local BossTab = Window:Tab({Title = "BOSS FIGHT", Icon = "skull"})
local TeleportTab = Window:Tab({Title = "Teleports", Icon = "map-pin"})
local MiscTab = Window:Tab({Title = "Misc", Icon = "settings"})
local RocksTab = Window:Tab({Title = "Rocks", Icon = "mountain"})
local KillingTab = Window:Tab({Title = "KILLING", Icon = "swords"})
local StatTab = Window:Tab({Title = "STAT", Icon = "bar-chart-2"})
local StatSpyTab = Window:Tab({Title = "STAT SPY", Icon = "scan-eye"})
local CreditsTab = Window:Tab({Title = "Credits", Icon = "info"})

MainTab:Section({Title = "Exercise Farm"})

-- Exercise tools from DAN public source
local exerciseList = {"Weight", "Pushups", "Situps", "Handstands", "Fast Punch", "Stomp", "Ground Slam"}
getgenv().selectedExercise = "Weight"
getgenv().autoExercise = false

local function equipExerciseTool(toolName)
    local char = LocalPlayer.Character
    local backpack = LocalPlayer:FindFirstChild("Backpack")
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    if not char or not hum then
        return false
    end
    if char:FindFirstChild(toolName) then
        return true
    end
    if backpack then
        local tool = backpack:FindFirstChild(toolName)
        if tool then
            hum:EquipTool(tool)
            return true
        end
        -- giveTool fallback
        local give = RemoteEvents and RemoteEvents:FindFirstChild("giveTool")
        if give then
            pcall(function()
                give:FireServer(toolName)
            end)
            task.wait(0.1)
            tool = backpack:FindFirstChild(toolName) or char:FindFirstChild(toolName)
            if tool and tool.Parent == backpack then
                hum:EquipTool(tool)
            end
            return tool ~= nil
        end
    end
    return false
end

local function doExerciseRep()
    local player = LocalPlayer
    local toolName = getgenv().selectedExercise or "Weight"
    local ev = player:FindFirstChild("muscleEvent") or MuscleEvent
    if toolName == "Fast Punch" then
        equipExerciseTool("Punch")
        if ev then
            ev:FireServer("punch", "rightHand")
            ev:FireServer("punch", "leftHand")
        end
        local punch = player.Character and player.Character:FindFirstChild("Punch")
        if punch then
            punch:Activate()
        end
    elseif toolName == "Stomp" then
        equipExerciseTool("Stomp")
        if ev then
            ev:FireServer("stomp")
        end
        local t = player.Character and player.Character:FindFirstChild("Stomp")
        if t then
            t:Activate()
        end
    elseif toolName == "Ground Slam" then
        equipExerciseTool("Ground Slam")
        if ev then
            ev:FireServer("slam")
        end
        local t = player.Character and player.Character:FindFirstChild("Ground Slam")
        if t then
            t:Activate()
        end
    else
        -- Weight / Pushups / Situps / Handstands
        equipExerciseTool(toolName)
        if ev then
            ev:FireServer("rep")
        end
    end
end

MainTab:Dropdown({
    Title = "Select Exercise",
    Values = exerciseList,
    Value = "Weight",
    Callback = function(Value)
        getgenv().selectedExercise = Value
    end
})

MainTab:Toggle({
    Title = "Auto Exercise",
    Desc = "Equip selected tool and train",
    Value = false,
    Callback = function(Value)
        getgenv().autoExercise = Value
        if Value then
            task.spawn(function()
                while getgenv().autoExercise do
                    pcall(doExerciseRep)
                    task.wait()
                end
            end)
        end
    end
})

MainTab:Section({Title = "Auto Farm Settings"})

local function getNearestMachineCFrame()
    local folder = workspace:FindFirstChild("machinesFolder")
    local myChar = LocalPlayer.Character
    local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not folder or not myRoot then
        return nil
    end
    local best, bestDist = nil, math.huge
    for _, obj in ipairs(folder:GetDescendants()) do
        if obj:IsA("BasePart") then
            local d = (obj.Position - myRoot.Position).Magnitude
            if d < bestDist then
                bestDist = d
                best = obj
            end
        end
    end
    if best then
        return best.CFrame * CFrame.new(0, 0, 3)
    end
    return nil
end

local function fireRep()
    local ev = LocalPlayer:FindFirstChild("muscleEvent") or MuscleEvent
    if ev then
        ev:FireServer("rep")
    end
end

MainTab:Toggle({
    Title = "Auto Fast Rep",
    Desc = "No delay - very fast training",
    Value = false,
    Callback = function(Value)
        getgenv().autoRep = Value
        if Value then
            task.spawn(function()
                while getgenv().autoRep do
                    pcall(function()
                        unequipProtein()
                        for i = 1, 25 do
                            fireRep()
                        end
                    end)
                    task.wait()
                end
            end)
        end
    end
})

MainTab:Toggle({
    Title = "Auto Fast Machine Lift",
    Desc = "Machine + no delay ultra fast rep",
    Value = false,
    Callback = function(Value)
        getgenv().autoMachineLift = Value
        if Value then
            task.spawn(function()
                while getgenv().autoMachineLift do
                    pcall(function()
                        unequipProtein()
                        local cf = getNearestMachineCFrame()
                        local char = LocalPlayer.Character
                        local root = char and char:FindFirstChild("HumanoidRootPart")
                        if cf and root then
                            root.CFrame = cf
                        end
                        for i = 1, 30 do
                            fireRep()
                        end
                    end)
                    task.wait()
                end
            end)
        end
    end
})

local function invokeREvent(remoteName, ...)
    local rEvents = ReplicatedStorage:FindFirstChild("rEvents")
    local remote = rEvents and rEvents:FindFirstChild(remoteName)
    if remote and remote:IsA("RemoteFunction") then
        return remote:InvokeServer(...)
    end
end

-- Muscle Gym Auto TP position from source (cframe9)
local MUSCLE_KING_GYM_CF = CFrame.new(
    -8751.244141, 120.948402, -5860.950195,
    0.28799805, -1.3e-7, 0.957630992,
    2.8e-8, 1, 1.27e-7,
    -0.957630992, -9e-9, 0.28799805
)

-- Muscle King Mountain rock from source rock table
local KING_ROCK_CF = CFrame.new(
    -9037.712891, 5.536252, -6058.991699,
    0.160788447, 7e-8, -0.986988902,
    4.7e-8, 1, 7.9e-8,
    0.986988902, -5.9e-8, 0.160788447
)

getgenv().autoKing = false
getgenv().autoMuscleGymTp = false

getgenv().rebirthTarget = 0 -- 0 = infinite

MainTab:Toggle({
    Title = "Fast Auto Rebirth",
    Desc = "Auto rebirth every 0.5s (need Strength)",
    Value = false,
    Callback = function(Value)
        getgenv().autoRebirth = Value
        if Value then
            task.spawn(function()
                local ls = LocalPlayer:FindFirstChild("leaderstats")
                local rebirths = ls and ls:FindFirstChild("Rebirths")
                while getgenv().autoRebirth do
                    pcall(function()
                        local target = tonumber(getgenv().rebirthTarget) or 0
                        if target > 0 and rebirths and rebirths.Value >= target then
                            getgenv().autoRebirth = false
                            WindUI:Notify({
                                Title = "Auto Rebirth",
                                Content = "Reached target " .. tostring(target),
                                Duration = 3
                            })
                            return
                        end
                        -- exact DAN public call
                        ReplicatedStorage.rEvents.rebirthRemote:InvokeServer("rebirthRequest")
                    end)
                    task.wait(0.5)
                end
            end)
            WindUI:Notify({
                Title = "Auto Rebirth",
                Content = "ON | 0.5s | needs enough Strength",
                Duration = 3
            })
        end
    end
})

--------------------------------------------------------------------
-- PACK FARM (Swift Samurai / Tribal Overlord source)
--------------------------------------------------------------------
local TRAINING_PET = "Swift Samurai"
local REBIRTH_PET = "Tribal Overlord"
local PET_STACK = 8

getgenv().packFastFarm = false
getgenv().packFastRebirth = false
getgenv().packFarmGen = 0
getgenv().packRebGen = 0

local function packMuscleEv()
    return LocalPlayer:FindFirstChild("muscleEvent") or MuscleEvent
end

local function packEquipRemote()
    return (RemoteEvents and RemoteEvents:FindFirstChild("equipPetEvent"))
        or (ReplicatedStorage:FindFirstChild("rEvents") and ReplicatedStorage.rEvents:FindFirstChild("equipPetEvent"))
end

local function packPetsFolder()
    return LocalPlayer:FindFirstChild("petsFolder")
end

local function packGetRequiredStrength()
    local ls = LocalPlayer:FindFirstChild("leaderstats")
    local reb = ls and ls:FindFirstChild("Rebirths")
    local v = reb and tonumber(reb.Value) or 0
    return 5000 + v * 2550
end

local function packGetStrength()
    local ls = LocalPlayer:FindFirstChild("leaderstats")
    local s = ls and ls:FindFirstChild("Strength")
    return s and tonumber(s.Value) or 0
end

local function packGetPetsByName(name)
    local pets = {}
    local folder = packPetsFolder()
    if not folder then
        return pets
    end
    local target = string.lower(name)
    for _, sub in ipairs(folder:GetChildren()) do
        if sub:IsA("Folder") then
            for _, pet in ipairs(sub:GetChildren()) do
                if string.lower(pet.Name) == target then
                    pets[#pets + 1] = pet
                end
            end
        end
    end
    return pets
end

local function packUnequipAll()
    local equip = packEquipRemote()
    local folder = packPetsFolder()
    if not equip or not folder then
        return
    end
    for _, sub in ipairs(folder:GetChildren()) do
        if sub:IsA("Folder") then
            for _, pet in ipairs(sub:GetChildren()) do
                pcall(function()
                    equip:FireServer("unequipPet", pet)
                end)
            end
        end
    end
    task.wait(0.15)
end

local function packEquipAmount(petName, amount)
    local equip = packEquipRemote()
    if not equip then
        return 0
    end
    local pets = packGetPetsByName(petName)
    local equipped = 0
    for _, pet in ipairs(pets) do
        if equipped >= amount then
            break
        end
        pcall(function()
            equip:FireServer("equipPet", pet)
        end)
        equipped = equipped + 1
        task.wait(0.04)
    end
    return equipped
end

local function packEquipTraining()
    packUnequipAll()
    return packEquipAmount(TRAINING_PET, PET_STACK)
end

local function packEquipRebirthPets()
    packUnequipAll()
    return packEquipAmount(REBIRTH_PET, PET_STACK)
end

local function packTrainFast()
    local ev = packMuscleEv()
    if not ev then
        return
    end
    for _ = 1, 30 do
        pcall(function()
            ev:FireServer("rep")
        end)
    end
end

local function packTrainRebirth()
    local ev = packMuscleEv()
    if not ev then
        return
    end
    for _ = 1, 10 do
        pcall(function()
            ev:FireServer("rep")
        end)
    end
end

local function packDoRebirth()
    local required = packGetRequiredStrength()
    if packGetStrength() < required then
        return false
    end
    packEquipRebirthPets()
    task.wait(0.25)
    local ls = LocalPlayer:FindFirstChild("leaderstats")
    local rebStat = ls and ls:FindFirstChild("Rebirths")
    local old = rebStat and rebStat.Value or 0
    local started = tick()
    local remote = ReplicatedStorage:FindFirstChild("rEvents")
        and ReplicatedStorage.rEvents:FindFirstChild("rebirthRemote")
    while getgenv().packFastRebirth and rebStat and rebStat.Value <= old and tick() - started < 8 do
        pcall(function()
            if remote then
                remote:InvokeServer("rebirthRequest")
            end
        end)
        task.wait(0.05)
    end
    return rebStat and rebStat.Value > old
end

local function startPackFastFarm()
    getgenv().packFarmGen = (getgenv().packFarmGen or 0) + 1
    local gen = getgenv().packFarmGen
    task.spawn(function()
        packEquipTraining()
        while getgenv().packFastFarm and gen == getgenv().packFarmGen do
            packTrainFast()
            task.wait(0.01)
        end
    end)
end

local function startPackFastRebirth()
    getgenv().packRebGen = (getgenv().packRebGen or 0) + 1
    local gen = getgenv().packRebGen
    task.spawn(function()
        packEquipTraining()
        while getgenv().packFastRebirth and gen == getgenv().packRebGen do
            local required = packGetRequiredStrength()
            if packGetStrength() < required then
                packTrainRebirth()
                task.wait(0.02)
            else
                local ok = packDoRebirth()
                if not getgenv().packFastRebirth or gen ~= getgenv().packRebGen then
                    break
                end
                task.wait(ok and 0.15 or 0.25)
                packEquipTraining()
            end
        end
    end)
end

PackFarmTab:Section({Title = "REBIRTHS"})
PackFarmTab:Paragraph({
    Title = "Pets",
    Desc = "Training: 8 Swift Samurai | Rebirth: 8 Tribal Overlord"
})

PackFarmTab:Toggle({
    Title = "Fast Farm",
    Desc = "Equip Swift Samurai + fast rep",
    Value = false,
    Callback = function(Value)
        getgenv().packFastFarm = Value
        getgenv().packFarmGen = (getgenv().packFarmGen or 0) + 1
        if Value then
            if getgenv().packFastRebirth then
                getgenv().packFastRebirth = false
                getgenv().packRebGen = (getgenv().packRebGen or 0) + 1
            end
            startPackFastFarm()
        end
    end
})

PackFarmTab:Toggle({
    Title = "Fast Rebirth",
    Desc = "Train -> Tribal Overlord x8 -> rebirth when strength ready",
    Value = false,
    Callback = function(Value)
        getgenv().packFastRebirth = Value
        getgenv().packRebGen = (getgenv().packRebGen or 0) + 1
        if Value then
            if getgenv().packFastFarm then
                getgenv().packFastFarm = false
                getgenv().packFarmGen = (getgenv().packFarmGen or 0) + 1
            end
            startPackFastRebirth()
        end
    end
})

PackFarmTab:Button({
    Title = "Equip training pets (8 Swift Samurai)",
    Callback = function()
        local n = packEquipTraining()
        WindUI:Notify({Title = "Pack", Content = n .. "/8 Swift Samurai", Duration = 3})
    end
})

PackFarmTab:Button({
    Title = "Equip rebirth pets (8 Tribal Overlord)",
    Callback = function()
        local n = packEquipRebirthPets()
        WindUI:Notify({Title = "Pack", Content = n .. "/8 Tribal Overlord", Duration = 3})
    end
})

MainTab:Toggle({
    Title = "Auto King (Rock Farm)",
    Desc = "Stay on Muscle King Mountain and punch",
    Value = false,
    Callback = function(Value)
        getgenv().autoKing = Value
        if Value then
            task.spawn(function()
                while getgenv().autoKing do
                    pcall(function()
                        local char = LocalPlayer.Character
                        local root = char and char:FindFirstChild("HumanoidRootPart")
                        if root then
                            root.CFrame = KING_ROCK_CF * CFrame.new(0, 0, 4)
                        end
                        local punch = equipPunch()
                        if punch then
                            punch:Activate()
                        end
                        local ev = LocalPlayer:FindFirstChild("muscleEvent") or MuscleEvent
                        if ev then
                            ev:FireServer("punch")
                            ev:FireServer("rep")
                        end
                    end)
                    task.wait(0.1)
                end
            end)
        end
    end
})

MainTab:Toggle({
    Title = "Muscle Gym Auto TP",
    Desc = "Keep TP to Muscle King Gym",
    Value = false,
    Callback = function(Value)
        getgenv().autoMuscleGymTp = Value
        if Value then
            task.spawn(function()
                while getgenv().autoMuscleGymTp do
                    task.wait(0.2)
                    pcall(function()
                        local char = LocalPlayer.Character
                        local root = char and char:FindFirstChild("HumanoidRootPart")
                        if root then
                            root.CFrame = MUSCLE_KING_GYM_CF
                        end
                    end)
                end
            end)
        end
    end
})

MainTab:Button({
    Title = "TP Muscle King Gym",
    Callback = function()
        pcall(function()
            local char = LocalPlayer.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            if root then
                root.CFrame = MUSCLE_KING_GYM_CF
            end
        end)
    end
})

-- Pets tab
local allPets = {
    -- Industrial / Apex update
    "Apex Overlord", "Core Pup", "Plasma Ravager", "Reactor Beast", "Titan Reactor", "Volt Talon",
    -- Radioactive / Ultra Titans packs (Sept update pets)
    "Biohazard Ape", "Biohazard Overlord", "Nuclear Hydra", "Omega Overlord",
    "Powercore Hound", "Radio Rat", "Sludge Croc", "Titanium Hydra", "Toxic Hound",
    -- Jungle / older shop pets
    "Aether Spirit Bunny", "Blue Birdie", "Blue Bunny", "Blue Firecaster", "Blue Phoenix",
    "Crimson Falcon", "Cybernetic Showdown Dragon", "Dark Golem", "Dark Legends Manticore",
    "Dark Vampy", "Darkstar Hunter", "Eternal Strike Leviathan", "Frostwave Legends Penguin",
    "Golden Phoenix", "Golden Viking", "Green Butterfly", "Infernal Dragon", "Jungle Captain",
    "Lightning Strike Phantom", "Magic Butterfly", "Mighty Monster", "Muscle Sensei",
    "Neon Guardian", "Orange Hedgehog", "Orange Pegasus", "Phantom Genesis Dragon",
    "Purple Dragon", "Purple Falcon", "Red Dragon", "Red Firecaster", "Red Kitty", "Silver Dog",
    "Swift Samurai", "Tribal Overlord", "Ultimate Supernova Pegasus", "Ultra Birdie",
    "White Pegasus", "Wild Wizards", "Yellow Butterfly"
}
table.sort(allPets)

local allAuras = {
    "Advanced Aura", "Astral Electro", "Azure Tundra", "Azure Tundra Aura", "Basic Aura",
    "Blue Aura", "Dark Electro", "Electro", "Enchanted Mirage", "Entropic Blast",
    "Entropic Blast Aura", "Epic Aura", "Eternal Megastrike Aura", "Grand SuperNova Aura",
    "Grand Supernova", "Green Aura", "Lightning", "Muscle King Aura", "Power Lightning",
    "Purple Aura", "Purple Nova", "Rare Aura", "Red Aura", "Supernova", "Ultra Inferno",
    "Ultra Inferno Aura", "Ultra Mirage", "Unique Aura", "Unstable Mirage", "Yellow Aura"
}
table.sort(allAuras)

-- merge live shop folder names from shared.runtime or root
pcall(function()
    local folders = {}
    local shared = ReplicatedStorage:FindFirstChild("shared")
    local runtime = shared and shared:FindFirstChild("runtime")
    if runtime and runtime:FindFirstChild("cPetShopFolder") then
        table.insert(folders, runtime.cPetShopFolder)
    end
    if ReplicatedStorage:FindFirstChild("cPetShopFolder") then
        table.insert(folders, ReplicatedStorage.cPetShopFolder)
    end
    local seen = {}
    for _, n in ipairs(allPets) do
        seen[n] = true
    end
    for _, folder in ipairs(folders) do
        for _, child in ipairs(folder:GetChildren()) do
            if not seen[child.Name] then
                allPets[#allPets + 1] = child.Name
                seen[child.Name] = true
            end
        end
    end
    table.sort(allPets)
end)

getgenv().selectedBuyPet = allPets[1]
getgenv().selectedHatchAura = allAuras[1]
getgenv().autoBuyPet = false
getgenv().autoHatchAura = false
getgenv().autoSellAll = false
getgenv().autoEvolveAll = false

local function buyPetShop(petName)
    if not petName then
        return false
    end
    local ok = false
    pcall(function()
        local remote = ReplicatedStorage:FindFirstChild("rEvents")
            and ReplicatedStorage.rEvents:FindFirstChild("cPetShopRemote")
        if not remote then
            remote = ReplicatedStorage:FindFirstChild("cPetShopRemote")
        end
        local petObj = nil
        local shared = ReplicatedStorage:FindFirstChild("shared")
        local runtime = shared and shared:FindFirstChild("runtime")
        local folder = runtime and runtime:FindFirstChild("cPetShopFolder")
        if folder then
            petObj = folder:FindFirstChild(petName)
        end
        if not petObj then
            folder = ReplicatedStorage:FindFirstChild("cPetShopFolder")
            petObj = folder and folder:FindFirstChild(petName)
        end
        if remote and petObj then
            if remote:IsA("RemoteFunction") then
                remote:InvokeServer(petObj)
            else
                remote:FireServer(petObj)
            end
            ok = true
            return
        end
        -- fallback: DAN public style root remote + folder
        if not remote then
            remote = ReplicatedStorage:FindFirstChild("cPetShopRemote")
        end
        if remote and petObj and remote:IsA("RemoteFunction") then
            remote:InvokeServer(petObj)
            ok = true
        end
    end)
    return ok
end

-- keep old multi-try path disabled; single correct InvokeServer only
local function buyPetShop_DISABLED_OLD(petName)
    if not petName then
        return false
    end
    local ok = false
    pcall(function()
        local folder = ReplicatedStorage:FindFirstChild("cPetShopFolder") or PetShopFolder
        local remote = ReplicatedStorage:FindFirstChild("cPetShopRemote") or PetShopRemote
        if not remote then
            return
        end
        local petObj = folder and (folder:FindFirstChild(petName) or folder:FindFirstChild(petName, true))
        if remote:IsA("RemoteFunction") then
            if petObj then
                pcall(function() remote:InvokeServer(petObj) end)
            end
            ok = true
        elseif remote:IsA("RemoteEvent") then
            if petObj then
                pcall(function() remote:FireServer(petObj) end)
                pcall(function() remote:FireServer("buy", petObj) end)
            end
            pcall(function() remote:FireServer(petName) end)
            pcall(function() remote:FireServer("buy", petName) end)
            ok = true
        end
    end)
    return ok
end

local function buyAura(auraName)
    if not auraName then
        return false
    end
    local ok = false
    pcall(function()
        local remote = ReplicatedStorage:FindFirstChild("cPetShopRemote")
            or ReplicatedStorage:FindFirstChild("cAuraShopRemote")
            or (RemoteEvents and RemoteEvents:FindFirstChild("buyAuraRemote"))
        if remote and remote:IsA("RemoteFunction") then
            pcall(function() remote:InvokeServer(auraName) end)
            pcall(function() remote:InvokeServer("buy", auraName) end)
            pcall(function() remote:InvokeServer("buyAura", auraName) end)
            ok = true
        elseif remote and remote:IsA("RemoteEvent") then
            pcall(function() remote:FireServer(auraName) end)
            pcall(function() remote:FireServer("buyAura", auraName) end)
            ok = true
        end
    end)
    return ok
end

local function sellAllPets()
    pcall(function()
        local sellRemote = RemoteEvents and RemoteEvents:FindFirstChild("sellPetEvent")
        local petsFolder = LocalPlayer:FindFirstChild("petsFolder")
        if not sellRemote or not petsFolder then
            return
        end
        for _, rarityFolder in ipairs(petsFolder:GetChildren()) do
            for _, pet in ipairs(rarityFolder:GetChildren()) do
                sellRemote:FireServer("sellPet", pet)
            end
        end
    end)
end

local function evolveAllPets()
    pcall(function()
        local evolveRemote = RemoteEvents and RemoteEvents:FindFirstChild("petEvolveEvent")
        local petsFolder = LocalPlayer:FindFirstChild("petsFolder")
        if not evolveRemote or not petsFolder then
            return
        end
        for _, rarityFolder in ipairs(petsFolder:GetChildren()) do
            for _, pet in ipairs(rarityFolder:GetChildren()) do
                evolveRemote:FireServer("evolvePet", pet)
                evolveRemote:FireServer("evolveTitan", pet)
            end
        end
    end)
end

PetTab:Section({Title = "Auto Buy Pet"})
PetTab:Paragraph({
    Title = "Pet Shop",
    Desc = "Select a pet A-Z then buy."
})

PetTab:Dropdown({
    Title = "Pet selector (A-Z)",
    Values = allPets,
    Value = allPets[1],
    Callback = function(Value)
        getgenv().selectedBuyPet = Value
    end
})

PetTab:Button({
    Title = "Buy selected pet",
    Callback = function()
        local name = getgenv().selectedBuyPet
        local ok = buyPetShop(name)
        WindUI:Notify({
            Title = "Buy Pet",
            Content = ok and ("Bought request: " .. tostring(name)) or "Buy failed",
            Duration = 2
        })
    end
})

PetTab:Toggle({
    Title = "Auto buy pet",
    Desc = "Keeps buying the selected pet",
    Value = false,
    Callback = function(Value)
        getgenv().autoBuyPet = Value
        if Value then
            task.spawn(function()
                while getgenv().autoBuyPet do
                    buyPetShop(getgenv().selectedBuyPet)
                    task.wait(0.2)
                end
            end)
        end
    end
})

PetTab:Section({Title = "Aura"})

PetTab:Dropdown({
    Title = "Aura selector (A-Z)",
    Values = allAuras,
    Value = allAuras[1],
    Callback = function(Value)
        getgenv().selectedHatchAura = Value
    end
})

PetTab:Button({
    Title = "Buy selected aura",
    Callback = function()
        local name = getgenv().selectedHatchAura
        local ok = buyAura(name)
        WindUI:Notify({
            Title = "Buy Aura",
            Content = ok and ("Requested: " .. tostring(name)) or "Aura remote missing",
            Duration = 2
        })
    end
})

PetTab:Toggle({
    Title = "Auto buy aura",
    Desc = "Keeps buying the selected aura",
    Value = false,
    Callback = function(Value)
        getgenv().autoHatchAura = Value
        if Value then
            task.spawn(function()
                while getgenv().autoHatchAura do
                    buyAura(getgenv().selectedHatchAura)
                    task.wait(0.2)
                end
            end)
        end
    end
})

PetTab:Section({Title = "Sell / Evolve"})

PetTab:Toggle({
    Title = "Auto sell all pets",
    Value = false,
    Callback = function(Value)
        getgenv().autoSellAll = Value
        if Value then
            task.spawn(function()
                while getgenv().autoSellAll do
                    sellAllPets()
                    task.wait(0.6)
                end
            end)
        end
    end
})

PetTab:Toggle({
    Title = "Auto evolve all pets",
    Value = false,
    Callback = function(Value)
        getgenv().autoEvolveAll = Value
        if Value then
            task.spawn(function()
                while getgenv().autoEvolveAll do
                    evolveAllPets()
                    task.wait(0.6)
                end
            end)
        end
    end
})

PetTab:Button({
    Title = "Sell all pets once",
    Callback = function()
        sellAllPets()
        WindUI:Notify({Title = "Pets", Content = "Sold all pets", Duration = 2})
    end
})

PetTab:Button({
    Title = "Evolve all pets once",
    Callback = function()
        evolveAllPets()
        WindUI:Notify({Title = "Pets", Content = "Evolve all sent", Duration = 2})
    end
})

--------------------------------------------------------------------
-- BOSS FIGHT TAB
--------------------------------------------------------------------
BossTab:Section({Title = "Boss Fight"})
BossTab:Paragraph({
    Title = "Auto Boss",
    Desc = "Low lag lock + attack (no chest)"
})

local bossRarities = {"Any", "Common", "Rare", "Epic", "Legendary", "Mythic", "Rainbow"}
getgenv().selectedBossRarity = "Any"
getgenv().autoKillBoss = false
getgenv().bossSpawnInfo = "Scanning..."
getgenv().lockedBoss = nil
getgenv().bossHeight = 68
getgenv().bossRange = 10
getgenv()._bossCache = nil
getgenv()._bossCacheT = 0
getgenv()._bossParts = nil

local playerNameSet = {}
local function refreshPlayerNames()
    playerNameSet = {}
    for _, plr in ipairs(Players:GetPlayers()) do
        playerNameSet[plr.Name] = true
        if plr.Character then
            playerNameSet[plr.Character.Name] = true
        end
    end
end

local function getBossRoot(model)
    if not model then
        return nil
    end
    return model:FindFirstChild("HumanoidRootPart")
        or model:FindFirstChild("Torso")
        or model:FindFirstChild("UpperTorso")
        or model:FindFirstChild("Root")
        or model.PrimaryPart
        or model:FindFirstChildWhichIsA("BasePart", true)
end

local function isBossPet(model)
	if not model then return true end
	local n = string.lower(model.Name)
	if string.find(n, "pet") or string.find(n, "evolved") then
		return true
	end
	for _, d in ipairs(model:GetDescendants()) do
		if d:IsA("TextLabel") or d:IsA("TextButton") then
			local t = string.lower(tostring(d.Text or ""))
			if string.find(t, "pet") or string.find(t, "evolved") then
				return true
			end
		elseif d:IsA("BillboardGui") then
			for _, c in ipairs(d:GetDescendants()) do
				if c:IsA("TextLabel") or c:IsA("TextButton") then
					local t = string.lower(tostring(c.Text or ""))
					if string.find(t, "pet") or string.find(t, "evolved") then
						return true
					end
				end
			end
		end
	end
	local p = model.Parent
	for _ = 1, 8 do
		if not p then break end
		if string.find(string.lower(p.Name), "pet") then
			return true
		end
		p = p.Parent
	end
	return false
end

local function isLivingBoss(model)
	if not model or not model:IsA("Model") or not model.Parent then
		return false
	end
	if model == LocalPlayer.Character then
		return false
	end
	if Players:GetPlayerFromCharacter(model) then
		return false
	end
	if playerNameSet[model.Name] then
		return false
	end
	if isBossPet(model) then
		return false
	end
	local hum = model:FindFirstChildOfClass("Humanoid")
	if not hum or hum.Health <= 0 then
		return false
	end
	local root = getBossRoot(model)
	if not root or not root.Parent then
		return false
	end
	if root.Position.Y < -50 then
		return false
	end
	return true, hum, root
end

local function clearBossTarget()
	getgenv().lockedBoss = nil
	getgenv()._bossCache = nil
	getgenv()._bossParts = nil
end

local function rarityMatches(name)
	local rarity = getgenv().selectedBossRarity or "Any"
	if rarity == "Any" then
		return true
	end
	local n, r = string.lower(name), string.lower(rarity)
	if r == "legendary" or r == "legend" then
		return string.find(n, "legend") ~= nil
	end
	if r == "mythic" or r == "myth" then
		return string.find(n, "myth") ~= nil
	end
	if r == "rainbow" then
		return string.find(n, "rainbow") ~= nil or string.find(n, "admin") ~= nil
	end
	return string.find(n, r) ~= nil
end

-- Real boss fights always have this hitbox. Pets do not.
local function findBossHitbox(boss)
	if boss then
		for _, n in ipairs({"BossDamageHitbox", "BossHitbox", "DamageHitbox"}) do
			local hb = boss:FindFirstChild(n, true)
			if hb and hb:IsA("BasePart") then
				return hb
			end
		end
	end
	for _, n in ipairs({"BossDamageHitbox", "BossHitbox"}) do
		local hb = workspace:FindFirstChild(n, true)
		if hb and hb:IsA("BasePart") then
			return hb
		end
	end
	return nil
end

local function modelNearPart(part, maxDist)
	if not part then
		return nil
	end
	local best, bestD = nil, maxDist or 80
	for _, obj in ipairs(workspace:GetDescendants()) do
		if obj:IsA("Humanoid") and obj.Health > 0 then
			local model = obj.Parent
			if model and model:IsA("Model") and isLivingBoss(model) then
				local root = getBossRoot(model)
				if root then
					local d = (root.Position - part.Position).Magnitude
					if d < bestD then
						bestD = d
						best = model
					end
				end
			end
		end
	end
	return best
end

local function findBossModel()
	refreshPlayerNames()

	if getgenv().lockedBoss and getgenv().lockedBoss.Parent and isLivingBoss(getgenv().lockedBoss) then
		local hb = findBossHitbox(getgenv().lockedBoss)
		-- keep lock only if real boss hitbox still exists nearby or on model
		if hb or findBossHitbox() then
			return getgenv().lockedBoss
		end
	end
	getgenv().lockedBoss = nil

	-- 1) hitbox first = only when a real boss fight is active
	local hb = findBossHitbox()
	if hb then
		local model = hb:FindFirstAncestorOfClass("Model")
		if model and isLivingBoss(model) then
			getgenv().lockedBoss = model
			return model
		end
		local near = modelNearPart(hb, 120)
		if near then
			getgenv().lockedBoss = near
			return near
		end
	end

	-- 2) named boss models (still require not pet + high hp)
	for _, name in ipairs({
		"Boss", "BossArena", "CommonBoss", "RareBoss", "EpicBoss",
		"LegendaryBoss", "MythicBoss", "RainbowBoss", "CurrentBoss"
	}) do
		local m = workspace:FindFirstChild(name, true)
		if m then
			if not m:IsA("Model") then
				m = m:FindFirstAncestorOfClass("Model") or m
			end
			if m:IsA("Model") and isLivingBoss(m) then
				local hum = m:FindFirstChildOfClass("Humanoid")
				if hum and hum.MaxHealth >= 1000 then
					getgenv().lockedBoss = m
					return m
				end
			end
		end
	end

	-- 3) folders that only exist for boss content
	for _, folderName in ipairs({"Bosses", "BossArena", "Boss", "Raids", "Raid"}) do
		local folder = workspace:FindFirstChild(folderName)
		if folder then
			for _, obj in ipairs(folder:GetDescendants()) do
				if obj:IsA("Model") and isLivingBoss(obj) then
					local hum = obj:FindFirstChildOfClass("Humanoid")
					if hum and hum.MaxHealth >= 1000 then
						getgenv().lockedBoss = obj
						return obj
					end
				end
			end
		end
	end

	-- no real boss up (lobby / pets only)
	return nil
end

local function bossPunch()
	local punch = equipPunch()
	local ev = LocalPlayer:FindFirstChild("muscleEvent") or MuscleEvent
	if punch then
		pcall(function()
			punch:Activate()
		end)
	end
	if ev then
		ev:FireServer("punch", "leftHand")
		ev:FireServer("punch", "rightHand")
		ev:FireServer("punch")
	end
end

local function bossPunchFast()
	-- keep Punch equipped forever while fighting; low-lag remote spam
	local char = LocalPlayer.Character
	local punch = char and char:FindFirstChild("Punch")
	if not punch then
		punch = equipPunch()
	end
	local ev = LocalPlayer:FindFirstChild("muscleEvent") or MuscleEvent
	if punch then
		pcall(function() punch:Activate() end)
	end
	if ev then
		for _ = 1, 8 do
			ev:FireServer("punch", "leftHand")
			ev:FireServer("punch", "rightHand")
			ev:FireServer("punch")
		end
	end
end

local function bossHeavy()
    local ev = LocalPlayer:FindFirstChild("muscleEvent") or MuscleEvent
    if not ev then
        return
    end
    pcall(function()
        local bag = LocalPlayer:FindFirstChild("Backpack")
        local char = LocalPlayer.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if bag and hum then
            local stomp = bag:FindFirstChild("Stomp") or char:FindFirstChild("Stomp")
            local slam = bag:FindFirstChild("Ground Slam") or char:FindFirstChild("Ground Slam")
            if stomp and stomp.Parent == bag then
                hum:EquipTool(stomp)
            elseif slam and slam.Parent == bag then
                hum:EquipTool(slam)
            end
        end
        ev:FireServer("stomp")
        ev:FireServer("slam")
        equipPunch()
    end)
end

local function ensureBossAntiKnock(myRoot)
    if not myRoot then
        return
    end
    local bv = myRoot:FindFirstChild("DAN_BOSS_BV")
    if not bv then
        bv = Instance.new("BodyVelocity")
        bv.Name = "DAN_BOSS_BV"
        bv.MaxForce = Vector3.new(1e5, 1e5, 1e5)
        bv.P = 1250
        bv.Parent = myRoot
    end
    bv.Velocity = Vector3.new(0, 0, 0)
end

local function clearBossAntiKnock(myRoot)
    if myRoot then
        local bv = myRoot:FindFirstChild("DAN_BOSS_BV")
        if bv then
            bv:Destroy()
        end
    end
end

local function bossStick(myRoot, bossRoot)
	local h = tonumber(getgenv().bossHeight) or 68
	local range = tonumber(getgenv().bossRange) or 6
	-- stay above boss and follow
	local pos = bossRoot.Position + Vector3.new(0, h, 0)
	myRoot.CFrame = CFrame.new(pos, bossRoot.Position)
	pcall(function()
		myRoot.AssemblyLinearVelocity = Vector3.zero
	end)
	ensureBossAntiKnock(myRoot)
end

local function bossDodge(myRoot, bossRoot)
    local h = tonumber(getgenv().bossHeight) or 10
    local range = tonumber(getgenv().bossRange) or 8
    local side = Vector3.new(1, 0, 0)
    local away = (myRoot.Position - bossRoot.Position)
    if away.Magnitude > 0.1 then
        side = away:Cross(Vector3.new(0, 1, 0))
        if side.Magnitude > 0.1 then
            side = side.Unit
        end
    end
    local dir = (math.random(0, 1) == 0) and 1 or -1
    local pos = bossRoot.Position + side * (range + 6) * dir + Vector3.new(0, h, 0)
    myRoot.CFrame = CFrame.new(pos, bossRoot.Position)
    ensureBossAntiKnock(myRoot)
end

-- Cache boss body parts (no workspace scan every frame = less lag)
local function refreshBossParts(bossModel, hitbox)
	local targets = {}
	if hitbox then
		targets[#targets + 1] = hitbox
	end
	if bossModel then
		for _, p in ipairs(bossModel:GetDescendants()) do
			if p:IsA("BasePart") then
				targets[#targets + 1] = p
			end
		end
	end
	getgenv()._bossParts = targets
	return targets
end

local function ensureHugeHitbox(myRoot)
	if not myRoot then return end
	local box = myRoot:FindFirstChild("DAN_HUGE_HB")
	if not box then
		box = Instance.new("Part")
		box.Name = "DAN_HUGE_HB"
		box.Anchored = false
		box.CanCollide = false
		box.Massless = true
		box.Transparency = 1
		box.Size = Vector3.new(80, 80, 80)
		box.Parent = myRoot
		local w = Instance.new("WeldConstraint")
		w.Part0 = myRoot
		w.Part1 = box
		w.Parent = box
	end
	box.CFrame = myRoot.CFrame
	return box
end

local function hitBossCached(myRoot)
	local targets = getgenv()._bossParts
	if not myRoot or not targets then return end
	local huge = ensureHugeHitbox(myRoot)
	if firetouchinterest then
		for i = 1, #targets do
			local part = targets[i]
			if part and part.Parent then
				pcall(firetouchinterest, part, myRoot, 0)
				pcall(firetouchinterest, part, myRoot, 1)
				if huge then
					pcall(firetouchinterest, part, huge, 0)
					pcall(firetouchinterest, part, huge, 1)
				end
			end
		end
	end
end

local function bossHitCycle(myRoot, targetPart, boss, hitbox)
	if not myRoot or not targetPart then return end
	equipPunch()
	-- always follow boss at height
	bossStick(myRoot, targetPart)
	hitBossCached(myRoot)
	bossPunchFast()
	-- quick touch then stay high
	myRoot.CFrame = CFrame.new(targetPart.Position + Vector3.new(0, 6, 0), targetPart.Position)
	hitBossCached(myRoot)
	bossPunchFast()
	bossStick(myRoot, targetPart)
	hitBossCached(myRoot)
	bossPunchFast()
end

-- Hide game boss reward / menu popup (the panel in screenshot)
getgenv().hideBossUI = true
local function hideBossPopup()
    if getgenv().hideBossUI == false then
        return
    end
    local pg = LocalPlayer:FindFirstChild("PlayerGui")
    if not pg then
        return
    end
    for _, gui in ipairs(pg:GetChildren()) do
        local n = string.lower(gui.Name)
        if string.find(n, "boss") or string.find(n, "reward") or string.find(n, "chest") then
            pcall(function()
                if gui:IsA("ScreenGui") or gui:IsA("GuiMain") then
                    gui.Enabled = false
                end
            end)
        end
        for _, d in ipairs(gui:GetDescendants()) do
            if d:IsA("TextLabel") or d:IsA("TextButton") then
                local t = string.lower(d.Text or "")
                if string.find(t, "click for rewards") or string.find(t, "chest rewards")
                    or string.find(t, "common boss") or string.find(t, "spawn rate") then
                    local root = d
                    for _ = 1, 8 do
                        if root.Parent and root.Parent ~= pg then
                            root = root.Parent
                        else
                            break
                        end
                    end
                    pcall(function()
                        if root:IsA("ScreenGui") then
                            root.Enabled = false
                        elseif root:IsA("Frame") or root:IsA("ImageLabel") then
                            root.Visible = false
                        end
                    end)
                end
            end
        end
    end
end

BossTab:Dropdown({
    Title = "Boss rarity filter",
    Values = bossRarities,
    Value = "Any",
    Callback = function(Value)
        getgenv().selectedBossRarity = Value
        getgenv().lockedBoss = nil
    end
})

BossTab:Dropdown({
    Title = "Fight height",
    Values = {"54", "58", "62", "68", "72"},
    Value = "68",
    Callback = function(Value)
        local n = tonumber(Value)
        if n then
            getgenv().bossHeight = n
        end
    end
})

BossTab:Dropdown({
    Title = "Distance from boss",
    Values = {"6", "8", "10", "12", "15"},
    Value = "10",
    Callback = function(Value)
        local n = tonumber(Value)
        if n then
            getgenv().bossRange = n
        end
    end
})

BossTab:Button({
    Title = "Find & TP to boss",
    Callback = function()
        getgenv().lockedBoss = nil
        local boss = findBossModel()
        local hitbox = findBossHitbox(boss)
        local myRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        local root = boss and getBossRoot(boss) or hitbox
        if root and myRoot then
            refreshBossParts(boss, hitbox)
            bossStick(myRoot, root)
        end
        hideBossPopup()
    end
})

BossTab:Toggle({
    Title = "Auto Boss",
    Desc = "Low lag follow + fast punch",
    Value = false,
    Callback = function(Value)
        getgenv().autoKillBoss = Value
        if Value then
            task.spawn(function()
                local lastHealth = nil
                local tickN = 0
                local cachedBoss, cachedHitbox, cachedRoot = nil, nil, nil
                local lastScan, lastPopup, lastEquip = 0, 0, 0
                while getgenv().autoKillBoss do
                    pcall(function()
                        local now = tick()
                        -- scan boss less often (less lag)
                        if now - lastScan > 0.35 or not cachedBoss or not cachedBoss.Parent then
                            lastScan = now
                            cachedBoss = findBossModel()
                            cachedHitbox = findBossHitbox(cachedBoss)
                            if cachedBoss and isLivingBoss(cachedBoss) then
                                cachedRoot = getBossRoot(cachedBoss)
                                if now - lastScan > 0.34 then
                                    refreshBossParts(cachedBoss, cachedHitbox)
                                end
                            else
                                cachedBoss, cachedHitbox, cachedRoot = nil, cachedHitbox, nil
                            end
                        end

                        if now - lastPopup > 2 then
                            lastPopup = now
                            hideBossPopup()
                        end

                        local boss, hitbox = cachedBoss, cachedHitbox
                        if not boss and not hitbox then
                            clearBossTarget()
                            return
                        end
                        if boss and not isLivingBoss(boss) then
                            clearBossTarget()
                            cachedBoss = nil
                            return
                        end

                        local char = LocalPlayer.Character
                        local myRoot = char and char:FindFirstChild("HumanoidRootPart")
                        local myHum = char and char:FindFirstChildOfClass("Humanoid")
                        if not myRoot then
                            return
                        end

                        local targetPart = cachedRoot or (boss and getBossRoot(boss)) or hitbox
                        if not targetPart or not targetPart.Parent then
                            clearBossTarget()
                            cachedBoss = nil
                            return
                        end
                        if targetPart.Position.Y < -50 then
                            clearBossTarget()
                            cachedBoss = nil
                            return
                        end

                        -- keep Punch equipped the whole fight
                        local held = char and char:FindFirstChild("Punch")
                        if not held then
                            equipPunch()
                        end

                        bossStick(myRoot, targetPart)

                        -- only touch hitbox (not every boss part)
                        if hitbox and firetouchinterest then
                            pcall(firetouchinterest, hitbox, myRoot, 0)
                            pcall(firetouchinterest, hitbox, myRoot, 1)
                            local huge = myRoot:FindFirstChild("DAN_HUGE_HB")
                            if huge then
                                pcall(firetouchinterest, hitbox, huge, 0)
                                pcall(firetouchinterest, hitbox, huge, 1)
                            else
                                ensureHugeHitbox(myRoot)
                            end
                        else
                            hitBossCached(myRoot)
                        end

                        -- fast punch (optimized)
                        bossPunchFast()

                        if myHum and lastHealth and myHum.Health < lastHealth - 2 then
                            bossDodge(myRoot, targetPart)
                            bossStick(myRoot, targetPart)
                        end
                        if myHum then
                            lastHealth = myHum.Health
                        end
                    end)
                    -- tight loop for punch rate, scans already throttled
                    task.wait(0.03)
                end
            end)
        else
            clearBossTarget()
            local myRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if myRoot then
                local bv = myRoot:FindFirstChild("DAN_BOSS_BV")
                if bv then bv:Destroy() end
            end
        end
    end
})

BossTab:Toggle({
    Title = "Hide boss popup UI",
    Desc = "Hides reward / boss menu panel",
    Value = true,
    Callback = function(Value)
        getgenv().hideBossUI = Value
        if Value then
            hideBossPopup()
        end
    end
})

local bossSpawnParagraph = BossTab:Paragraph({
    Title = "Boss status / spawn",
    Desc = "Scanning..."
})

task.spawn(function()
    while true do
        local info = "No timer found"
        pcall(function()
            local found = {}
            local boss = findBossModel()
            if boss then
                local hum = boss:FindFirstChildOfClass("Humanoid")
                local hp = hum and (math.floor(hum.Health) .. "/" .. math.floor(hum.MaxHealth)) or "?"
                table.insert(found, "TARGET: " .. boss.Name .. " HP " .. hp)
            end
            for _, obj in ipairs(workspace:GetDescendants()) do
                local n = string.lower(obj.Name)
                if string.find(n, "boss") or string.find(n, "spawn") or string.find(n, "timer") or string.find(n, "raid") then
                    if obj:IsA("StringValue") or obj:IsA("NumberValue") or obj:IsA("IntValue") then
                        table.insert(found, obj.Name .. "=" .. tostring(obj.Value))
                    elseif obj:IsA("TextLabel") or obj:IsA("TextButton") then
                        local t = obj.Text
                        if t and #t > 0 and #t < 80 then
                            local low = string.lower(t)
                            if string.find(low, "boss") or string.find(low, "spawn") or string.find(t, ":") then
                                table.insert(found, t)
                            end
                        end
                    end
                end
            end
            local pg = LocalPlayer:FindFirstChild("PlayerGui")
            if pg then
                for _, obj in ipairs(pg:GetDescendants()) do
                    if obj:IsA("TextLabel") or obj:IsA("TextButton") then
                        local t = obj.Text or ""
                        local low = string.lower(t)
                        if (string.find(low, "boss") or string.find(low, "spawn") or string.find(low, "raid")) and #t < 100 then
                            table.insert(found, t)
                        end
                    end
                end
            end
            if #found > 0 then
                info = table.concat(found, " | ")
                if #info > 200 then
                    info = string.sub(info, 1, 200) .. "..."
                end
            else
                info = "No active boss / timer not found"
            end
        end)
        getgenv().bossSpawnInfo = info
        pcall(function()
            if bossSpawnParagraph and bossSpawnParagraph.SetDesc then
                bossSpawnParagraph:SetDesc(info)
            end
        end)
        task.wait(3)
    end
end)

BossTab:Button({
    Title = "Refresh boss status",
    Callback = function()
        WindUI:Notify({
            Title = "Boss Status",
            Content = tostring(getgenv().bossSpawnInfo or "Scanning..."),
            Duration = 4
        })
    end
})

TeleportTab:Section({Title = "Teleport Locations"})

for _, gymInfo in ipairs(gyms) do
    TeleportTab:Button({
        Title = "Teleport to " .. gymInfo[1],
        Callback = function()
            teleportTo(gymInfo[2])
        end
    })
end

MiscTab:Section({Title = "Quests & Rewards"})

MiscTab:Toggle({
    Title = "Auto Claim Quest",
    Desc = "Automatically claims available quests",
    Value = false,
    Callback = function(Value)
        getgenv().autoQuest = Value
        if Value then
            task.spawn(function()
                while getgenv().autoQuest do
                    pcall(function()
                        local r = RemoteEvents:FindFirstChild("questRemote")
                            or RemoteEvents:FindFirstChild("claimQuestRemote")
                            or RemoteEvents:FindFirstChild("questsEvent")
                        if r then
                            if r:IsA("RemoteEvent") then
                                r:FireServer("claimQuest")
                                r:FireServer("claimAll")
                            else
                                r:InvokeServer("claimQuest")
                                r:InvokeServer("claimAll")
                            end
                        end
                    end)
                    task.wait(2)
                end
            end)
        end
    end
})

MiscTab:Toggle({
    Title = "Auto Spin (Fortune)",
    Desc = "Automatically spins the daily fortune wheel",
    Value = false,
    Callback = function(Value)
        getgenv().autoSpin = Value
        if Value then
            task.spawn(function()
                while getgenv().autoSpin do
                    pcall(function()
                        local Event = ReplicatedStorage.rEvents.openFortuneWheelRemote
                        local chances = ReplicatedStorage.shared.catalogs.fortuneWheelChances["Fortune Wheel"]
                        Event:InvokeServer("openFortuneWheel", chances)
                    end)
                    task.wait(3)
                end
            end)
        end
    end
})

MiscTab:Section({Title = "Tools"})

MiscTab:Toggle({
    Title = "Double Equip (Weight + Pushups)",
    Desc = "Keeps both Weight and Pushups ready",
    Value = false,
    Callback = function(Value)
        getgenv().doubleEquip = Value
        if Value then
            task.spawn(function()
                while getgenv().doubleEquip do
                    pcall(function()
                        local char = LocalPlayer.Character
                        local bp = LocalPlayer:FindFirstChild("Backpack")
                        if not char or not bp then
                            return
                        end
                        local w = bp:FindFirstChild("Weight") or char:FindFirstChild("Weight")
                        local p = bp:FindFirstChild("Pushups")
                            or char:FindFirstChild("Pushups")
                            or bp:FindFirstChild("Push Ups")
                            or char:FindFirstChild("Push Ups")
                        if w and w.Parent == bp then
                            char.Humanoid:EquipTool(w)
                            task.wait(0.15)
                        end
                        if p and p.Parent == bp then
                            char.Humanoid:EquipTool(p)
                        end
                    end)
                    task.wait(0.8)
                end
            end)
        end
    end
})

RocksTab:Section({Title = "MUST HAVE THE REQUIREMENT FOR THE ROCK"})
RocksTab:Paragraph({
    Title = "Requirement Info",
    Desc = "You need enough Durability. Only enable 1 rock at a time."
})
RocksTab:Section({Title = "Auto Punch Rocks"})

RocksTab:Paragraph({Title = "Tiny Island Rock", Desc = "Requirement: 0 Durability"})
RocksTab:Toggle({
    Title = "Auto Punch Tiny Island Rock",
    Value = false,
    Callback = function(Value)
        getgenv().punchTiny = Value
        if Value then
            punchRockLoop("punchTiny", "Tiny Rock")
        end
    end
})

RocksTab:Paragraph({Title = "Frozen Island Rock", Desc = "Requirement: 150K Durability"})
RocksTab:Toggle({
    Title = "Auto Punch Frozen Island Rock",
    Value = false,
    Callback = function(Value)
        getgenv().punchFrozen = Value
        if Value then
            punchRockLoop("punchFrozen", "Frozen Rock")
        end
    end
})

RocksTab:Paragraph({Title = "Mythical Island Rock", Desc = "Requirement: 400K Durability"})
RocksTab:Toggle({
    Title = "Auto Punch Mythical Island Rock",
    Value = false,
    Callback = function(Value)
        getgenv().punchMythical = Value
        if Value then
            punchRockLoop("punchMythical", "Mythical Rock")
        end
    end
})

RocksTab:Paragraph({Title = "Hell Island Rock", Desc = "Requirement: 750K Durability"})
RocksTab:Toggle({
    Title = "Auto Punch Hell Island Rock",
    Value = false,
    Callback = function(Value)
        getgenv().punchHell = Value
        if Value then
            punchRockLoop("punchHell", "Inferno Rock")
        end
    end
})

RocksTab:Paragraph({Title = "Legend Rock", Desc = "Requirement: 1M Durability"})
RocksTab:Toggle({
    Title = "Auto Punch Legend Rock",
    Value = false,
    Callback = function(Value)
        getgenv().punchLegend = Value
        if Value then
            punchRockLoop("punchLegend", "Rock Of Legends")
        end
    end
})

RocksTab:Paragraph({Title = "Muscle King Rock", Desc = "Requirement: 5M Durability"})
RocksTab:Toggle({
    Title = "Auto Punch Muscle King Rock",
    Value = false,
    Callback = function(Value)
        getgenv().punchMuscleKing = Value
        if Value then
            punchRockLoop("punchMuscleKing", "Muscle King Mountain")
        end
    end
})

RocksTab:Paragraph({Title = "Jungle Rock", Desc = "Requirement: 10M Durability"})
RocksTab:Toggle({
    Title = "Auto Punch Jungle Rock",
    Value = false,
    Callback = function(Value)
        getgenv().punchJungle = Value
        if Value then
            punchRockLoop("punchJungle", "Ancient Jungle Rock")
        end
    end
})

RocksTab:Paragraph({Title = "Industrial Rock", Desc = "Requirement: Check in-game"})
RocksTab:Toggle({
    Title = "Auto Punch Industrial Rock",
    Value = false,
    Callback = function(Value)
        getgenv().punchIndustrial = Value
        if Value then
            punchRockLoop("punchIndustrial", "Industrial Rock")
        end
    end
})


--------------------------------------------------------------------
-- STAT TRACKING (local player gains)
--------------------------------------------------------------------
local function readLocalStats()
    local stats = {
        Strength = 0,
        Durability = 0,
        Rebirths = 0,
        Kills = 0,
        Brawls = 0
    }
    pcall(function()
        local ls = LocalPlayer:FindFirstChild("leaderstats")
        if ls then
            for _, v in ipairs(ls:GetChildren()) do
                local n = string.lower(v.Name)
                local val = tonumber(v.Value) or 0
                if string.find(n, "stren") then
                    stats.Strength = val
                elseif string.find(n, "dura") then
                    stats.Durability = val
                elseif string.find(n, "rebirth") then
                    stats.Rebirths = val
                elseif string.find(n, "kill") then
                    stats.Kills = val
                elseif string.find(n, "brawl") then
                    stats.Brawls = val
                end
            end
        end
        -- Muscle Legends often stores more under player folders
        for _, folderName in ipairs({"MuscleStats", "stats", "Stats", "playerStats"}) do
            local f = LocalPlayer:FindFirstChild(folderName)
            if f then
                for _, v in ipairs(f:GetDescendants()) do
                    if v:IsA("NumberValue") or v:IsA("IntValue") then
                        local n = string.lower(v.Name)
                        local val = tonumber(v.Value) or 0
                        if string.find(n, "stren") and val > stats.Strength then
                            stats.Strength = val
                        elseif string.find(n, "dura") and val > stats.Durability then
                            stats.Durability = val
                        elseif string.find(n, "rebirth") and val > stats.Rebirths then
                            stats.Rebirths = val
                        end
                    end
                end
            end
        end
    end)
    return stats
end

local function formatNum(n)
    n = tonumber(n) or 0
    local abs = math.abs(n)
    if abs >= 1e12 then
        return string.format("%.2fT", n / 1e12)
    elseif abs >= 1e9 then
        return string.format("%.2fB", n / 1e9)
    elseif abs >= 1e6 then
        return string.format("%.2fM", n / 1e6)
    elseif abs >= 1e3 then
        return string.format("%.2fK", n / 1e3)
    end
    return string.format("%.0f", n)
end

local statStart = readLocalStats()
local statStartTime = os.clock()
local lastStatSnapshot = {
    Strength = statStart.Strength,
    Durability = statStart.Durability
}

local function ratesFor(delta, seconds)
    if seconds <= 0 then
        return 0, 0, 0, 0, 0, 0
    end
    local perSec = delta / seconds
    return perSec * 60, perSec * 3600, perSec * 86400, perSec * 604800, perSec * 2592000, perSec * 31536000
end

StatTab:Section({Title = "Your Gain Rates"})
StatTab:Paragraph({
    Title = "How it works",
    Desc = "Tracks Strength & Durability from when the script loaded. Farm to see rates update."
})

local strengthPara = StatTab:Paragraph({
    Title = "Strength Rates",
    Desc = "Loading..."
})
local duraPara = StatTab:Paragraph({
    Title = "Durability Rates",
    Desc = "Loading..."
})
local summaryPara = StatTab:Paragraph({
    Title = "Session Summary",
    Desc = "Loading..."
})

StatTab:Button({
    Title = "Reset STAT Timer",
    Callback = function()
        statStart = readLocalStats()
        statStartTime = os.clock()
        lastStatSnapshot = {
            Strength = statStart.Strength,
            Durability = statStart.Durability
        }
        WindUI:Notify({
            Title = "STAT",
            Content = "Timer reset!",
            Duration = 2
        })
    end
})

StatTab:Button({
    Title = "Refresh STAT Now",
    Callback = function()
        local now = readLocalStats()
        local elapsed = math.max(os.clock() - statStartTime, 1)
        local dStr = now.Strength - statStart.Strength
        local dDur = now.Durability - statStart.Durability
        local sMin, sHr, sDay, sWk, sMo, sYr = ratesFor(dStr, elapsed)
        local dMin, dHr, dDay, dWk, dMo, dYr = ratesFor(dDur, elapsed)
        pcall(function()
            strengthPara:SetDesc(
                "Per min: " .. formatNum(sMin) ..
                "\nPer hour: " .. formatNum(sHr) ..
                "\nPer day: " .. formatNum(sDay) ..
                "\nPer week: " .. formatNum(sWk) ..
                "\nPer month: " .. formatNum(sMo) ..
                "\nPer year: " .. formatNum(sYr)
            )
        end)
        pcall(function()
            duraPara:SetDesc(
                "Per min: " .. formatNum(dMin) ..
                "\nPer hour: " .. formatNum(dHr) ..
                "\nPer day: " .. formatNum(dDay) ..
                "\nPer week: " .. formatNum(dWk) ..
                "\nPer month: " .. formatNum(dMo) ..
                "\nPer year: " .. formatNum(dYr)
            )
        end)
        pcall(function()
            summaryPara:SetDesc(
                "Elapsed: " .. string.format("%.0fs", elapsed) ..
                "\nStrength gained: " .. formatNum(dStr) ..
                "\nDurability gained: " .. formatNum(dDur) ..
                "\nCurrent STR: " .. formatNum(now.Strength) ..
                "\nCurrent DUR: " .. formatNum(now.Durability)
            )
        end)
    end
})

task.spawn(function()
    while true do
        task.wait(3)
        local now = readLocalStats()
        local elapsed = math.max(os.clock() - statStartTime, 1)
        local dStr = now.Strength - statStart.Strength
        local dDur = now.Durability - statStart.Durability
        local sMin, sHr, sDay, sWk, sMo, sYr = ratesFor(dStr, elapsed)
        local dMin, dHr, dDay, dWk, dMo, dYr = ratesFor(dDur, elapsed)
        pcall(function()
            strengthPara:SetDesc(
                "Per min: " .. formatNum(sMin) ..
                " | Per hour: " .. formatNum(sHr) ..
                " | Per day: " .. formatNum(sDay) ..
                "\nPer week: " .. formatNum(sWk) ..
                " | Per month: " .. formatNum(sMo) ..
                " | Per year: " .. formatNum(sYr)
            )
        end)
        pcall(function()
            duraPara:SetDesc(
                "Per min: " .. formatNum(dMin) ..
                " | Per hour: " .. formatNum(dHr) ..
                " | Per day: " .. formatNum(dDay) ..
                "\nPer week: " .. formatNum(dWk) ..
                " | Per month: " .. formatNum(dMo) ..
                " | Per year: " .. formatNum(dYr)
            )
        end)
        pcall(function()
            summaryPara:SetDesc(
                "Elapsed: " .. string.format("%.0fs", elapsed) ..
                " | STR +" .. formatNum(dStr) ..
                " | DUR +" .. formatNum(dDur) ..
                "\nNow STR: " .. formatNum(now.Strength) ..
                " | Now DUR: " .. formatNum(now.Durability)
            )
        end)
    end
end)

--------------------------------------------------------------------
-- STAT SPY (other players)
--------------------------------------------------------------------
local function readPlayerStats(player)
    local stats = {
        Strength = nil,
        Durability = nil,
        Agility = nil,
        Rebirths = nil,
        Kills = nil,
        Brawls = nil
    }
    if not player then
        return stats
    end
    pcall(function()
        local function applyName(n, val)
            local low = string.lower(tostring(n))
            if string.find(low, "stren") then
                stats.Strength = val
            elseif string.find(low, "dura") then
                stats.Durability = val
            elseif string.find(low, "agil") or string.find(low, "speed") then
                stats.Agility = val
            elseif string.find(low, "rebirth") then
                stats.Rebirths = val
            elseif string.find(low, "kill") then
                stats.Kills = val
            elseif string.find(low, "brawl") then
                stats.Brawls = val
            else
                stats[tostring(n)] = val
            end
        end
        local ls = player:FindFirstChild("leaderstats")
        if ls then
            for _, v in ipairs(ls:GetChildren()) do
                if v:IsA("NumberValue") or v:IsA("IntValue") or v:IsA("StringValue") then
                    applyName(v.Name, v.Value)
                end
            end
        end
        for _, folderName in ipairs({"MuscleStats", "stats", "Stats", "playerStats", "Data", "data"}) do
            local f = player:FindFirstChild(folderName)
            if f then
                for _, v in ipairs(f:GetDescendants()) do
                    if v:IsA("NumberValue") or v:IsA("IntValue") then
                        applyName(v.Name, v.Value)
                    end
                end
            end
        end
        -- common alternate attribute names
        for _, attr in ipairs({"Durability", "durability", "Dura", "dura", "Strength", "strength", "Agility", "agility"}) do
            local ok, val = pcall(function()
                return player:GetAttribute(attr)
            end)
            if ok and val ~= nil then
                applyName(attr, val)
            end
        end
    end)
    return stats
end

local function playerList()
    local names = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            names[#names + 1] = p.Name
        end
    end
    table.sort(names)
    if #names == 0 then
        names[1] = "(no other players)"
    end
    return names
end

local selectedSpyPlayer = nil
local spyNames = playerList()

StatSpyTab:Section({Title = "Spy Other Players"})
StatSpyTab:Paragraph({
    Title = "Info",
    Desc = "Select a player then view their visible stats (leaderstats / public values)."
})

local spyDropdown = StatSpyTab:Dropdown({
    Title = "Select Player",
    Values = spyNames,
    Value = spyNames[1],
    Callback = function(Option)
        selectedSpyPlayer = Option
    end
})

local spyResult = StatSpyTab:Paragraph({
    Title = "Player Stats",
    Desc = "Select a player and press Refresh / Spy"
})

StatSpyTab:Button({
    Title = "Refresh Player List",
    Callback = function()
        spyNames = playerList()
        pcall(function()
            if spyDropdown.Refresh then
                spyDropdown:Refresh(spyNames)
            elseif spyDropdown.SetValues then
                spyDropdown:SetValues(spyNames)
            end
        end)
        selectedSpyPlayer = spyNames[1]
        WindUI:Notify({
            Title = "STAT SPY",
            Content = "Player list refreshed (" .. tostring(#spyNames) .. ")",
            Duration = 2
        })
    end
})

StatSpyTab:Button({
    Title = "Spy Selected Player",
    Callback = function()
        local name = selectedSpyPlayer or spyNames[1]
        if not name or name == "(no other players)" then
            WindUI:Notify({
                Title = "STAT SPY",
                Content = "No player selected",
                Duration = 2
            })
            return
        end
        local plr = Players:FindFirstChild(name)
        if not plr then
            WindUI:Notify({
                Title = "STAT SPY",
                Content = "Player left the server",
                Duration = 2
            })
            return
        end
        local stats = readPlayerStats(plr)
        local lines = {"Player: " .. plr.Name .. " (@" .. plr.DisplayName .. ")"}
        local order = {"Strength", "Durability", "Agility", "Rebirths", "Kills", "Brawls"}
        local shown = {}
        local count = 0
        for _, key in ipairs(order) do
            if stats[key] ~= nil then
                lines[#lines + 1] = key .. ": " .. formatNum(stats[key])
                shown[key] = true
                count = count + 1
            end
        end
        for k, v in pairs(stats) do
            if not shown[k] and v ~= nil then
                lines[#lines + 1] = tostring(k) .. ": " .. formatNum(v)
                count = count + 1
            end
        end
        if count == 0 then
            lines[#lines + 1] = "No public stats found (may be hidden server-side)"
        end
        if stats.Durability == nil then
            lines[#lines + 1] = "Durability: (not visible on this player)"
        end
        pcall(function()
            spyResult:SetDesc(table.concat(lines, "\n"))
        end)
        WindUI:Notify({
            Title = "STAT SPY",
            Content = "Spyed " .. plr.Name,
            Duration = 2
        })
    end
})

Players.PlayerAdded:Connect(function()
    task.wait(0.5)
    spyNames = playerList()
end)
Players.PlayerRemoving:Connect(function()
    task.wait(0.2)
    spyNames = playerList()
end)


-- ===================== KILLING TAB =====================
local function killPlayerList()
    local names = {}
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer then
            names[#names + 1] = plr.Name
        end
    end
    table.sort(names)
    if #names == 0 then
        names[1] = "No players"
    end
    return names
end

local function getPlayerByName(name)
    if not name or name == "No players" then
        return nil
    end
    return Players:FindFirstChild(name)
end

local function punchTarget(plr)
    if not plr or not plr.Character then
        return
    end
    local targetRoot = plr.Character:FindFirstChild("HumanoidRootPart")
    local myChar = LocalPlayer.Character
    local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not targetRoot or not myRoot then
        return
    end
    myRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 0, 3)
    local punch = equipPunch()
    if punch then
        punch:Activate()
    end
    if MuscleEvent then
        MuscleEvent:FireServer("punch")
        MuscleEvent:FireServer("rep")
    end
end

local function isWhitelisted(name)
    if not name then
        return false
    end
    for _, n in ipairs(getgenv().whitelistNames) do
        if n == name then
            return true
        end
    end
    return false
end

KillingTab:Section({Title = "Kill Selected Player"})

local killNames = killPlayerList()
local killDropdown = KillingTab:Dropdown({
    Title = "Player Selector to kill",
    Values = killNames,
    Value = killNames[1],
    Callback = function(Value)
        getgenv().selectedKillPlayer = Value
    end
})

KillingTab:Button({
    Title = "Refresh",
    Callback = function()
        killNames = killPlayerList()
        pcall(function()
            killDropdown:Refresh(killNames)
        end)
        WindUI:Notify({
            Title = "KILLING",
            Content = "Player list refreshed",
            Duration = 2
        })
    end
})

KillingTab:Toggle({
    Title = "Auto kill selected player",
    Value = false,
    Callback = function(Value)
        getgenv().autoKillSelected = Value
        if Value then
            task.spawn(function()
                while getgenv().autoKillSelected do
                    pcall(function()
                        local plr = getPlayerByName(getgenv().selectedKillPlayer)
                        if plr then
                            punchTarget(plr)
                        end
                    end)
                    task.wait(0.15)
                end
            end)
        end
    end
})

KillingTab:Section({Title = "Spectate"})

local spectateNames = killPlayerList()
local spectateDropdown = KillingTab:Dropdown({
    Title = "Select player here to spectate",
    Values = spectateNames,
    Value = spectateNames[1],
    Callback = function(Value)
        getgenv().selectedSpectatePlayer = Value
    end
})

KillingTab:Button({
    Title = "Refresh Spectate List",
    Callback = function()
        spectateNames = killPlayerList()
        pcall(function()
            spectateDropdown:Refresh(spectateNames)
        end)
        WindUI:Notify({
            Title = "KILLING",
            Content = "Spectate list refreshed",
            Duration = 2
        })
    end
})

KillingTab:Toggle({
    Title = "Spectate selected player",
    Value = false,
    Callback = function(Value)
        pcall(function()
            local cam = workspace.CurrentCamera
            if Value then
                local plr = getPlayerByName(getgenv().selectedSpectatePlayer)
                if plr and plr.Character then
                    local hum = plr.Character:FindFirstChildOfClass("Humanoid")
                    if hum then
                        cam.CameraSubject = hum
                    end
                end
            else
                local myChar = LocalPlayer.Character
                local hum = myChar and myChar:FindFirstChildOfClass("Humanoid")
                if hum then
                    cam.CameraSubject = hum
                end
            end
        end)
    end
})

KillingTab:Section({Title = "Auto Kill Everyone"})

KillingTab:Toggle({
    Title = "Auto kill everyone",
    Value = false,
    Callback = function(Value)
        getgenv().autoKillEveryone = Value
        if Value then
            task.spawn(function()
                while getgenv().autoKillEveryone do
                    pcall(function()
                        for _, plr in ipairs(Players:GetPlayers()) do
                            if not getgenv().autoKillEveryone then
                                break
                            end
                            if plr ~= LocalPlayer and not isWhitelisted(plr.Name) then
                                punchTarget(plr)
                                task.wait(0.12)
                            end
                        end
                    end)
                    task.wait(0.05)
                end
            end)
        end
    end
})

KillingTab:Section({Title = "Whitelist (safe from Auto kill everyone)"})

local wlNames = killPlayerList()
local wlDropdown = KillingTab:Dropdown({
    Title = "Whitelist SELECTOR",
    Values = wlNames,
    Value = wlNames[1],
    Callback = function(Value)
        getgenv().selectedWhitelistPlayer = Value
    end
})

KillingTab:Button({
    Title = "Refresh Whitelist List",
    Callback = function()
        wlNames = killPlayerList()
        pcall(function()
            wlDropdown:Refresh(wlNames)
        end)
        WindUI:Notify({
            Title = "KILLING",
            Content = "Whitelist list refreshed",
            Duration = 2
        })
    end
})

KillingTab:Button({
    Title = "Whitelist player",
    Callback = function()
        local name = getgenv().selectedWhitelistPlayer
        if name and name ~= "No players" and not isWhitelisted(name) then
            table.insert(getgenv().whitelistNames, name)
            WindUI:Notify({
                Title = "KILLING",
                Content = name .. " whitelisted (safe)",
                Duration = 2
            })
        end
    end
})

KillingTab:Button({
    Title = "Clear Whitelist",
    Callback = function()
        getgenv().whitelistNames = {}
        WindUI:Notify({
            Title = "KILLING",
            Content = "Whitelist cleared",
            Duration = 2
        })
    end
})

Players.PlayerAdded:Connect(function()
    task.wait(0.4)
    killNames = killPlayerList()
    spectateNames = killPlayerList()
    wlNames = killPlayerList()
end)
Players.PlayerRemoving:Connect(function()
    task.wait(0.2)
    killNames = killPlayerList()
    spectateNames = killPlayerList()
    wlNames = killPlayerList()
end)

CreditsTab:Section({Title = "Nova Hub | Muscle Legends"})
CreditsTab:Paragraph({Title = "Made by", Desc = "SGA / SUN GOD NIKA"})
CreditsTab:Paragraph({Title = "Version", Desc = "2.1"})
CreditsTab:Paragraph({Title = "Game", Desc = "Muscle Legends"})
CreditsTab:Paragraph({Title = "UI Library", Desc = "WindUI by Footagesus"})

CreditsTab:Button({
    Title = "Copy Creator Name",
    Callback = function()
        pcall(function()
            setclipboard("SGA")
        end)
        WindUI:Notify({
            Title = "Credits",
            Content = "SGA copied!",
            Duration = 2
        })
    end
})

WindUI:Notify({
    Title = "Nova Hub | Muscle Legends",
    Content = "v2.1 loaded",
    Duration = 4
})

print("Nova Hub v2.1 loaded | Made by SGA x AHMAD")
