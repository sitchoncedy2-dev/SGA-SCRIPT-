--[[

Ts Code Was Deobfuscated Using Slayerson âœžâœž Private Deobf
processed time. 622.94 Ms

Obfuscator: v1.0.0 https://wearedevs.net/obfuscator

-- ]]

repeat task.wait() until game:IsLoaded()

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local CORRECT_KEY = "DANHUB"
local GET_KEY_LINK = "https://discord.gg/vfGtDCSPn"

getgenv().KEY_PASSED = false

local Gui = Instance.new("ScreenGui", game.CoreGui)
Gui.Name = "DAN KEY"
Gui.ResetOnSpawn = false

local Main = Instance.new("Frame", Gui)
Main.Size = UDim2.new(0, 380, 0, 240)
Main.Position = UDim2.new(0.5, -190, 0.5, -120)
Main.BackgroundColor3 = Color3.fromRGB(15,15,15)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true

Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 14)

local Glow = Instance.new("UIStroke", Main)
Glow.Thickness = 2

local hue = 0
RunService.RenderStepped:Connect(function()
    hue = (hue + 0.002) % 1
    Glow.Color = Color3.fromHSV(hue, 1, 1)
end)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1,0,0,50)
Title.BackgroundTransparency = 1
Title.Text = "DAN HUB KEY SYSTEM BRO"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 20
Title.TextColor3 = Color3.fromRGB(255,255,255)

local Box = Instance.new("TextBox", Main)
Box.Size = UDim2.new(0.85,0,0,42)
Box.Position = UDim2.new(0.075,0,0.35,0)
Box.PlaceholderText = "ENTER KEY"
Box.Text = ""
Box.Font = Enum.Font.Gotham
Box.TextSize = 14
Box.BackgroundColor3 = Color3.fromRGB(30,30,30)
Box.TextColor3 = Color3.fromRGB(255,255,255)
Box.ClearTextOnFocus = false
Instance.new("UICorner", Box).CornerRadius = UDim.new(0,10)

local Verify = Instance.new("TextButton", Main)
Verify.Size = UDim2.new(0.4,0,0,38)
Verify.Position = UDim2.new(0.075,0,0.65,0)
Verify.Text = "VERIFY"
Verify.Font = Enum.Font.GothamBold
Verify.TextSize = 14
Verify.TextColor3 = Color3.fromRGB(255,255,255)
Verify.BackgroundColor3 = Color3.fromRGB(40,40,40)
Instance.new("UICorner", Verify).CornerRadius = UDim.new(0,10)

local GetKey = Instance.new("TextButton", Main)
GetKey.Size = UDim2.new(0.4,0,0,38)
GetKey.Position = UDim2.new(0.525,0,0.65,0)
GetKey.Text = "GET KEY"
GetKey.Font = Enum.Font.GothamBold
GetKey.TextSize = 14
GetKey.TextColor3 = Color3.fromRGB(255,255,255)
GetKey.BackgroundColor3 = Color3.fromRGB(40,40,40)
Instance.new("UICorner", GetKey).CornerRadius = UDim.new(0,10)

for _,btn in pairs({Verify, GetKey}) do
    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {
            BackgroundColor3 = Color3.fromRGB(70,70,70)
        }):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {
            BackgroundColor3 = Color3.fromRGB(40,40,40)
        }):Play()
    end)
end

local function Shake()
    for i = 1,8 do
        Main.Position += UDim2.new(0, (i%2==0 and -6 or 6),0,0)
        task.wait(0.02)
    end
end

Verify.MouseButton1Click:Connect(function()
    if Box.Text == CORRECT_KEY then
        getgenv().KEY_PASSED = true

        TweenService:Create(Main, TweenInfo.new(0.5), {
            BackgroundTransparency = 1
        }):Play()

        for _,v in pairs(Main:GetChildren()) do
            if v:IsA("TextLabel") or v:IsA("TextButton") or v:IsA("TextBox") then
                TweenService:Create(v, TweenInfo.new(0.5), {
                    TextTransparency = 1,
                    BackgroundTransparency = 1
                }):Play()
            end
        end

        task.wait(0.6)
        Gui:Destroy()
    else
        Box.Text = ""
        Box.PlaceholderText = "WRONG KEY!"
        Shake()
    end
end)

GetKey.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard(GET_KEY_LINK)
    end
end)

repeat task.wait() until getgenv().KEY_PASSED == true

-- DAN HUB | Legends Of Speed v1.0 | Owner: SUN GOD NIKA X IBLAMEKIO

local getgenv = getgenv or function()
    return _G
end

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualUser = game:GetService("VirtualUser")
local LocalPlayer = Players.LocalPlayer

local rEvents = ReplicatedStorage:WaitForChild("rEvents", 10)
local orbEvent = rEvents and rEvents:FindFirstChild("orbEvent")
local rebirthEvent = rEvents and rEvents:FindFirstChild("rebirthEvent")
local openCrystalRemote = rEvents and rEvents:FindFirstChild("openCrystalRemote")

getgenv().autoOrbs = false
getgenv().autoGems = false
getgenv().autoHoops = false
getgenv().autoRebirth = false
getgenv().autoRace = false
getgenv().orbArea = "City"
getgenv().farmMethod = "Remote"

local startTime = os.time()

local areas = {
    "City",
    "Snow City",
    "Magma City",
    "Legends Highway"
}

local crystals = {
    "Red Crystal",
    "Blue Crystal",
    "Purple Crystal",
    "Yellow Crystal",
    "Lightning Crystal",
    "Snow Crystal",
    "Lava Crystal",
    "Inferno Crystal",
    "Electro Legends Crystal",
    "Jungle Crystal"
}

LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

local function createAfkDisplay(character)
    if not character then
        return
    end
    local head = character:WaitForChild("Head", 5)
    if not head or head:FindFirstChild("AFK_Tag") then
        return
    end
    local billboard = Instance.new("BillboardGui")
    billboard.Name = "AFK_Tag"
    billboard.Size = UDim2.new(0, 170, 0, 28)
    billboard.StudsOffset = Vector3.new(0, 3, 0)
    billboard.AlwaysOnTop = true
    billboard.Parent = head
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = "DAN ANTI-AFK [00:00:00]"
    label.TextColor3 = Color3.fromRGB(0, 255, 200)
    label.Font = Enum.Font.GothamBold
    label.TextSize = 12
    label.TextStrokeTransparency = 0.2
    label.Parent = billboard
    local stroke = Instance.new("UIStroke")
    stroke.Thickness = 1.4
    stroke.Color = Color3.fromRGB(0, 0, 0)
    stroke.Parent = label
    task.spawn(function()
        while billboard and billboard.Parent do
            local e = os.time() - startTime
            local h = math.floor(e / 3600)
            local m = math.floor((e % 3600) / 60)
            local s = e % 60
            label.Text = string.format("DAN ANTI-AFK [%02d:%02d:%02d]", h, m, s)
            task.wait(1)
        end
    end)
end

if LocalPlayer.Character then
    createAfkDisplay(LocalPlayer.Character)
end
LocalPlayer.CharacterAdded:Connect(createAfkDisplay)

local function getRoot()
    local char = LocalPlayer.Character
    if not char then
        return nil
    end
    return char:FindFirstChild("HumanoidRootPart")
end

local function getHead()
    local char = LocalPlayer.Character
    if not char then
        return nil
    end
    return char:FindFirstChild("Head")
end

local function fireTouch(part)
    local head = getHead()
    if not head or not part then
        return
    end
    if firetouchinterest then
        pcall(function()
            firetouchinterest(head, part, 0)
            firetouchinterest(head, part, 1)
            firetouchinterest(head, part, 0)
        end)
    else
        local root = getRoot()
        if root and part:IsA("BasePart") then
            pcall(function()
                root.CFrame = part.CFrame
            end)
        end
    end
end

local function collectOrbRemote(orbName, area)
    if not orbEvent then
        return
    end
    pcall(function()
        orbEvent:FireServer("collectOrb", orbName, area)
    end)
end

local function collectOrbsTouch()
    local folder = workspace:FindFirstChild("orbFolder")
    if not folder then
        return
    end
    for _, v in ipairs(folder:GetDescendants()) do
        if v.Name == "TouchInterest" and v.Parent then
            fireTouch(v.Parent)
        elseif v:IsA("BasePart") and (string.find(v.Name, "Orb") or v.Name == "outerOrb") then
            fireTouch(v)
        end
    end
end

local function collectGemsTouch()
    local folder = workspace:FindFirstChild("orbFolder")
    if not folder then
        return
    end
    for _, v in ipairs(folder:GetDescendants()) do
        if v.Name == "outerGem" or v.Name == "Gem" then
            if v:IsA("BasePart") then
                fireTouch(v)
            elseif v:IsA("Model") then
                local p = v:FindFirstChildWhichIsA("BasePart")
                if p then
                    fireTouch(p)
                end
            end
        end
    end
end

local function collectHoopsTouch()
    local hoops = workspace:FindFirstChild("Hoops")
    if not hoops then
        return
    end
    for _, hoop in ipairs(hoops:GetDescendants()) do
        if hoop.Name == "TouchInterest" and hoop.Parent then
            fireTouch(hoop.Parent)
        elseif hoop:IsA("BasePart") and (hoop.Name == "Hoop" or string.find(string.lower(hoop.Name), "hoop")) then
            fireTouch(hoop)
        end
    end
end

local function doRebirth()
    if not rebirthEvent then
        return
    end
    pcall(function()
        rebirthEvent:FireServer("rebirthRequest")
    end)
end

local function openCrystal(name)
    if not openCrystalRemote then
        return
    end
    pcall(function()
        openCrystalRemote:InvokeServer("openCrystal", name)
    end)
end

local function claimChests()
    pcall(function()
        local chestEvent = rEvents and (rEvents:FindFirstChild("chestEvent") or rEvents:FindFirstChild("claimChestEvent") or rEvents:FindFirstChild("groupRewardEvent"))
        if chestEvent then
            if chestEvent:IsA("RemoteEvent") then
                chestEvent:FireServer("claimAll")
                chestEvent:FireServer("claimChest")
            else
                chestEvent:InvokeServer("claimAll")
            end
        end
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj:IsA("ClickDetector") and string.find(string.lower(obj.Parent and obj.Parent.Name or ""), "chest") then
                pcall(function()
                    fireclickdetector(obj)
                end)
            end
        end
    end)
end

local function tryJoinRace()
    pcall(function()
        local raceRemote = rEvents and (rEvents:FindFirstChild("raceEvent") or rEvents:FindFirstChild("joinRaceRemote") or rEvents:FindFirstChild("raceRemote"))
        if raceRemote then
            if raceRemote:IsA("RemoteEvent") then
                raceRemote:FireServer("joinRace")
                raceRemote:FireServer("startRace")
            else
                raceRemote:InvokeServer("joinRace")
            end
        end
    end)
end

local function tryWinRace()
    pcall(function()
        local raceRemote = rEvents and (rEvents:FindFirstChild("raceEvent") or rEvents:FindFirstChild("raceRemote"))
        if raceRemote then
            if raceRemote:IsA("RemoteEvent") then
                raceRemote:FireServer("winRace")
                raceRemote:FireServer("finishRace")
            else
                raceRemote:InvokeServer("winRace")
            end
        end
        local finish = workspace:FindFirstChild("raceFinish") or workspace:FindFirstChild("FinishLine")
        local root = getRoot()
        if root and finish then
            local part = finish:IsA("BasePart") and finish or finish:FindFirstChildWhichIsA("BasePart")
            if part then
                root.CFrame = part.CFrame
            end
        end
    end)
end

local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "DAN HUB",
    Author = "Legends Of Speed",
    Folder = "DANHUB_LOS",
    Size = UDim2.fromOffset(580, 520),
    Transparent = true,
    Theme = "Dark"
})

local MainTab = Window:Tab({Title = "Main", Icon = "home"})
local FarmTab = Window:Tab({Title = "Farm", Icon = "zap"})
local CrystalTab = Window:Tab({Title = "Crystals", Icon = "gem"})
local RaceTab = Window:Tab({Title = "Race", Icon = "flag"})
local MiscTab = Window:Tab({Title = "Misc", Icon = "settings"})
local CreditsTab = Window:Tab({Title = "Credits", Icon = "info"})

MainTab:Section({Title = "Farm Settings"})

MainTab:Dropdown({
    Title = "Orb Area",
    Values = areas,
    Value = "City",
    Callback = function(Option)
        getgenv().orbArea = Option
    end
})

MainTab:Dropdown({
    Title = "Farm Method",
    Values = {"Remote", "Touch", "Both"},
    Value = "Remote",
    Callback = function(Option)
        getgenv().farmMethod = Option
    end
})

FarmTab:Section({Title = "Auto Collect"})

FarmTab:Toggle({
    Title = "Auto Collect Orbs",
    Desc = "Farms steps / XP orbs",
    Value = false,
    Callback = function(Value)
        getgenv().autoOrbs = Value
        if Value then
            task.spawn(function()
                while getgenv().autoOrbs do
                    pcall(function()
                        local method = getgenv().farmMethod
                        local area = getgenv().orbArea or "City"
                        if method == "Remote" or method == "Both" then
                            collectOrbRemote("Red Orb", area)
                            collectOrbRemote("Orange Orb", area)
                            collectOrbRemote("Yellow Orb", area)
                        end
                        if method == "Touch" or method == "Both" then
                            collectOrbsTouch()
                        end
                    end)
                    task.wait(0.05)
                end
            end)
        end
    end
})

FarmTab:Toggle({
    Title = "Auto Collect Gems",
    Desc = "Farms gem orbs",
    Value = false,
    Callback = function(Value)
        getgenv().autoGems = Value
        if Value then
            task.spawn(function()
                while getgenv().autoGems do
                    pcall(function()
                        local method = getgenv().farmMethod
                        local area = getgenv().orbArea or "City"
                        if method == "Remote" or method == "Both" then
                            collectOrbRemote("Gem", area)
                        end
                        if method == "Touch" or method == "Both" then
                            collectGemsTouch()
                        end
                    end)
                    task.wait(0.05)
                end
            end)
        end
    end
})

FarmTab:Toggle({
    Title = "Auto Collect Hoops",
    Desc = "Touches hoops for XP",
    Value = false,
    Callback = function(Value)
        getgenv().autoHoops = Value
        if Value then
            task.spawn(function()
                while getgenv().autoHoops do
                    pcall(function()
                        collectHoopsTouch()
                    end)
                    task.wait(0.08)
                end
            end)
        end
    end
})

FarmTab:Section({Title = "Rebirth"})

FarmTab:Toggle({
    Title = "Auto Rebirth",
    Desc = "Spam rebirth request",
    Value = false,
    Callback = function(Value)
        getgenv().autoRebirth = Value
        if Value then
            task.spawn(function()
                while getgenv().autoRebirth do
                    doRebirth()
                    task.wait(0.35)
                end
            end)
        end
    end
})

MiscTab:Section({Title = "Chests"})

MiscTab:Button({
    Title = "Claim All Chests",
    Callback = function()
        claimChests()
        WindUI:Notify({
            Title = "DAN HUB",
            Content = "Tried to claim chests",
            Duration = 2
        })
    end
})

RaceTab:Section({Title = "Race"})

RaceTab:Button({
    Title = "Join Race",
    Callback = function()
        tryJoinRace()
        WindUI:Notify({
            Title = "Race",
            Content = "Join race requested",
            Duration = 2
        })
    end
})

RaceTab:Button({
    Title = "Instant Win Race",
    Callback = function()
        tryWinRace()
        WindUI:Notify({
            Title = "Race",
            Content = "Win race requested",
            Duration = 2
        })
    end
})

RaceTab:Toggle({
    Title = "Auto Join and Win Race",
    Value = false,
    Callback = function(Value)
        getgenv().autoRace = Value
        if Value then
            task.spawn(function()
                while getgenv().autoRace do
                    tryJoinRace()
                    task.wait(1)
                    tryWinRace()
                    task.wait(2)
                end
            end)
        end
    end
})

CrystalTab:Section({Title = "Instant Hatch"})

for _, crystalName in ipairs(crystals) do
    CrystalTab:Button({
        Title = crystalName,
        Callback = function()
            openCrystal(crystalName)
            WindUI:Notify({
                Title = "Crystal",
                Content = "Opened " .. crystalName,
                Duration = 2
            })
        end
    })
end

CreditsTab:Section({Title = "DAN HUB"})
CreditsTab:Paragraph({Title = "Owner", Desc = "SUN GOD NIKA X IBLAMEKIO"})
CreditsTab:Paragraph({Title = "Version", Desc = "1.0 - Legends Of Speed"})
CreditsTab:Paragraph({Title = "Game", Desc = "Legends Of Speed"})
CreditsTab:Paragraph({Title = "UI Library", Desc = "WindUI by Footagesus"})

CreditsTab:Button({
    Title = "Copy Owner Name",
    Callback = function()
        pcall(function()
            setclipboard("SUN GOD NIKA X IBLAMEKIO")
        end)
        WindUI:Notify({
            Title = "Credits",
            Content = "Owner name copied!",
            Duration = 2
        })
    end
})

WindUI:Notify({
    Title = "DAN HUB",
    Content = "Legends Of Speed v1.0 loaded",
    Duration = 4
})

print("DAN HUB | Legends Of Speed v1.0 loaded!")
